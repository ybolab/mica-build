# WiFi kernel modules for the cx3576 dual-SKU fit (one module name per line).
# Only one SKU is fitted per device; the driver for the absent chip fails to
# load and is skipped silently by mos-modules.
bcmdhd
aic8800_fdrv
