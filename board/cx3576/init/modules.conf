# Board hardware modules for cx3576 (one module name per line).
# WiFi/BT is the AIC8800D80 combo (single SKU; the dual-SKU bcmdhd fallback was
# dropped when the fleet was confirmed AIC-only). aic8800_btlpm drives the BT
# core and must be loaded before mos-bt attaches the HCI.
aic8800_fdrv
aic8800_btlpm
