# kernel project context allowlist (context = boards/cx3576/bsp)
*
!scripts/
!kernel/config/
!kernel/patches/
!kernel/dts/
!kernel/logo/
!kernel/configure.sh
!kernel/build.sh
# The boot-logo master, and nothing else out of rootfs/: that directory also
# holds the committed Wi-Fi firmware drop and the alpine bring-up root, which
# this build has no use for.
!rootfs/assets/splash.png
