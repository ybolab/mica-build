# The kernel patch series, in application order. scripts/apply-patches.sh reads
# this file, requires every entry to exist and every *.patch in this directory to
# be named here, and applies them with GNU `patch -p1`.
#
# 0004 must stay last of the device-tree entries: it registers the .dts that
# kernel/Dockerfile copies in beside this directory, so the Makefile it edits has
# to be the one the earlier patches left behind.
0001-motorcomm-yt8531-stock-init-sequence.patch
0002-wireless-makefile-gate-aic8800-by-aic-wlan-support.patch
0003-mali-deterministic-build-date.patch
0004-arm64-dts-rockchip-register-cx3576z-dtb.patch
0005-mpp-rkvenc2-rk3576-fixed-rate-opp.patch
0006-fbcon-retain-and-restore-board-logo.patch
