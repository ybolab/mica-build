--- a/bl33/v2023/drivers/amlogic/media/vout/hdmitx/hdmitx21/hdmitx_drv.c
+++ b/bl33/v2023/drivers/amlogic/media/vout/hdmitx/hdmitx21/hdmitx_drv.c
@@ -1683,7 +1683,9 @@
 	env_set("hdmichecksum", (const char *)checksum);
 	printf("hdmi_tx_set: mode: %s, attr: %s, save hdmichecksum: %s\n",
 	       env_get("outputmode"), env_get("colorattribute"), env_get("hdmichecksum"));
+#if !defined(CONFIG_S7D_BM201)
 	run_command("saveenv", 0);
+#endif
 }
 
 static void hdmitx_set_frlrate_none(struct hdmitx_dev *hdev)
--- a/bl33/v2023/drivers/amlogic/media/osd/dv/dolby_vision.c
+++ b/bl33/v2023/drivers/amlogic/media/osd/dv/dolby_vision.c
@@ -75,6 +75,13 @@
 u32 dv_cert_graphic_width = 1920;
 u32 dv_cert_graphic_height = 1080;
 
+static void persist_dolby_vision_state(void)
+{
+#if !defined(CONFIG_S7D_BM201)
+	run_command("saveenv", 0);
+#endif
+}
+
 #ifndef AML_S5_DISPLAY
 #define COEFF_NORM(a) ((int)((((a) * 2048.0) + 1) / 2))
 #define MATRIX_5x3_COEF_SIZE 24
@@ -1904,7 +1911,7 @@
 			dolby_vision_on = 1;
 			env_set("dolby_vision_on", "1");
 		}
-		run_command("saveenv", 0);
+		persist_dolby_vision_state();
 		printk("Dolby Vision turn on\n");
 	}
 	return 0;
@@ -2129,7 +2136,7 @@
 			} else {
 				printf("dolby_vision_process: no tv and dv disabled\n");
 				env_set("dolby_vision_on", "0");
-				run_command("saveenv", 0);
+				persist_dolby_vision_state();
 				return;
 			}
 		} else {
@@ -2140,14 +2147,14 @@
 				} else {
 					printf("dolby_vision_process: hpd: dv disabled\n");
 					env_set("dolby_vision_on", "0");
-					run_command("saveenv", 0);
+					persist_dolby_vision_state();
 					return;
 				}
 		}
 		if (m_dovi_setting.dst_format == FORMAT_INVALID) {
 			printf("dolby_vision_process: dst_format = FORMAT_INVALID\n");
 			env_set("dolby_vision_on", "0");
-			run_command("saveenv", 0);
+			persist_dolby_vision_state();
 			return;
 		}
 		if (dolby_vision_parse(hdev) == 0) {
@@ -2158,7 +2165,7 @@
 			m_dovi_setting.dst_format = FORMAT_INVALID;
 			dv_fw_valid = false;
 			env_set("dolby_vision_on", "0");
-			run_command("saveenv", 0);
+			persist_dolby_vision_state();
 			return;
 		}
 	} else {
@@ -2169,7 +2176,7 @@
 			} else {
 				printf("dolby_vision_process: no tv and dv disabled\n");
 				env_set("dolby_vision_on", "0");
-				run_command("saveenv", 0);
+				persist_dolby_vision_state();
 				return;
 			}
 		} else {
@@ -2180,14 +2187,14 @@
 			} else {
 				printf("dolby_vision_process: hpd: dv disabled\n");
 				env_set("dolby_vision_on", "0");
-				run_command("saveenv", 0);
+				persist_dolby_vision_state();
 				return;
 			}
 		}
 		if (dovi_setting.dst_format == FORMAT_INVALID) {
 			printf("dolby_vision_process: dst_format = FORMAT_INVALID\n");
 			env_set("dolby_vision_on", "0");
-			run_command("saveenv", 0);
+			persist_dolby_vision_state();
 			return;
 		}
 		if (dolby_vision_parse(hdev) == 0) {
@@ -2198,7 +2205,7 @@
 			dovi_setting.dst_format = FORMAT_INVALID;
 			dv_fw_valid = false;
 			env_set("dolby_vision_on", "0");
-			run_command("saveenv", 0);
+			persist_dolby_vision_state();
 			return;
 		}
 	}
