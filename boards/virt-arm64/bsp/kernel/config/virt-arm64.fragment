# virt-arm64 kernel configuration -- the board's own requirements, merged over
# arm64's `defconfig` after boards/common/mos-required.fragment.
#
# THIS FILE IS THE INTENT AND THE ASSERTION LIST. Every `=y` line here is
# asserted against the resolved `.config` after `olddefconfig`, so a symbol
# whose dependencies stopped being met fails the build instead of vanishing.
# config/virt-arm64.config beside it is the RESOLVED result, recorded so that a
# kernel bump shows up as a reviewable diff rather than as a build input nobody
# saw.
#
# `=y` and not `=m` throughout, and that is the point of the board: it boots
# root=/dev/dm-0 from a `dm-mod.create=` table with NO INITRAMFS, so nothing can
# load a module before the root filesystem exists. Everything the kernel needs
# in order to find, verify and mount the root is therefore built in.
#
# WHAT IS NOT HERE. The verity floor, the container-network floor and the
# security/LSM baseline are board-independent and live in
# boards/common/mos-required.fragment, which the build merges FIRST and asserts
# by the same loop. A line restated here would be a second statement free to
# disagree with the first.
#
# HOW THIS DIFFERS FROM boards/x64/bsp/kernel/config/x64.fragment. The two
# boards run the same software on the same boot contract, so the shape is
# identical and only the machine differs: this one is QEMU's aarch64 `virt`,
# which presents virtio over PCIe, a PL011 UART and nothing else. Every x86
# entry in the x64 fragment -- the Intel/Realtek NIC families, the SATA and
# NVMe controllers, USB storage, the EFI framebuffer, the x86 crypto
# accelerator -- has no counterpart here, and its absence is the statement that
# this board has no such hardware to drive.

# --- how the root device is found -------------------------------------------
# virtio, and only virtio. The `virt` machine has no SATA, no NVMe and no USB
# controller unless one is asked for, and nothing in this board's definition
# asks. x64 builds in three storage families because an industrial PC's media
# is the machine's choice; here the machine is a command line this repository
# writes, and it says virtio-blk-pci.
CONFIG_VIRTIO=y
CONFIG_VIRTIO_PCI=y
CONFIG_VIRTIO_MENU=y
CONFIG_VIRTIO_BLK=y
CONFIG_VIRTIO_MMIO=y
CONFIG_PCI=y
CONFIG_PCI_HOST_GENERIC=y
CONFIG_BLK_DEV=y

# --- how the partitions are read --------------------------------------------
# GPT, because every partition in boards/virt-arm64/board.env is addressed by
# its PARTUUID and dm-init resolves `PARTUUID=` through name_to_dev_t, which
# reads the GPT entry's own GUID. Without EFI_PARTITION the verity table names
# a device that never appears and the boot stops at rootwait.
CONFIG_PARTITION_ADVANCED=y
CONFIG_EFI_PARTITION=y
CONFIG_MSDOS_PARTITION=y

# --- the filesystems the layout mounts --------------------------------------
# ext4 for META, STATE, EPHEMERAL and DATA; vfat for the ESP and both boot
# partitions, which boot.mount mounts at /boot. The three NLS tables are what
# vfat needs to read a FAT directory at all -- without codepage 437 and
# iso8859-1 the mount fails with "IO charset ... not found", which on this
# board would be RAUC unable to reach grubenv.
CONFIG_EXT4_FS=y
CONFIG_EXT4_FS_POSIX_ACL=y
CONFIG_EXT4_FS_SECURITY=y
CONFIG_FAT_FS=y
CONFIG_VFAT_FS=y
CONFIG_NLS=y
CONFIG_NLS_CODEPAGE_437=y
CONFIG_NLS_ISO8859_1=y
CONFIG_NLS_ASCII=y
CONFIG_NLS_UTF8=y

# --- the network the virt machine has ---------------------------------------
# virtio-net and nothing else, for the reason the storage list is short: the
# only NIC this board ever sees is the one the harness gives it.
CONFIG_VIRTIO_NET=y

# --- the console boards/virt-arm64/board.env names --------------------------
# BOARD_CMDLINE_ARGS is `console=ttyAMA0,115200`, one entry and no tty0,
# because the aarch64 `virt` machine has no VGA and no framebuffer. ttyAMA0 is
# the PL011, which is the machine's only console, so both the driver and its
# console hook have to be built in or the argument names a console that is not
# there and a headless boot is indistinguishable from a hang.
#
# NOTE: cx3576 gives no precedent for this. Its config carries
# `# CONFIG_SERIAL_AMBA_PL011 is not set` -- a Rockchip board consoles through
# a vendor 8250 variant at ttyFIQ0 -- so the PL011 is new to this tree.
CONFIG_SERIAL_AMBA_PL011=y
CONFIG_SERIAL_AMBA_PL011_CONSOLE=y
CONFIG_TTY=y
CONFIG_UNIX98_PTYS=y

# --- firmware and boot ------------------------------------------------------
# The firmware (AAVMF) boots GRUB from the ESP and GRUB loads this kernel, so
# EFI_STUB is not on the boot path today. EFI itself is: efivarfs is how
# anything on the device reads or writes a UEFI variable, and systemd checks for
# it to decide whether the machine booted through firmware at all.
CONFIG_EFI=y
CONFIG_EFI_STUB=y
CONFIG_EFIVAR_FS=y

# --- what systemd needs to be pid 1 -----------------------------------------
# Not speculative: each of these is a hard requirement in systemd's own README,
# and the failure mode for most of them is a boot that reaches pid 1 and stops
# there rather than a message naming the symbol.
CONFIG_DEVTMPFS=y
CONFIG_DEVTMPFS_MOUNT=y
CONFIG_TMPFS=y
CONFIG_TMPFS_POSIX_ACL=y
CONFIG_TMPFS_XATTR=y
CONFIG_PROC_FS=y
CONFIG_SYSFS=y
CONFIG_CGROUPS=y
CONFIG_INOTIFY_USER=y
CONFIG_FANOTIFY=y
CONFIG_SIGNALFD=y
CONFIG_TIMERFD=y
CONFIG_EPOLL=y
CONFIG_NET=y
CONFIG_INET=y
CONFIG_IPV6=y
CONFIG_UNIX=y
CONFIG_NAMESPACES=y
CONFIG_NET_NS=y
CONFIG_USER_NS=y
CONFIG_PID_NS=y
CONFIG_IPC_NS=y
CONFIG_UTS_NS=y
CONFIG_SECCOMP=y
CONFIG_SECCOMP_FILTER=y
CONFIG_AUTOFS_FS=y
CONFIG_BLK_DEV_LOOP=y
# systemd-repart grows DATA on first boot by rewriting the GPT and asking the
# kernel to re-read it; without the partition rescan path that is a write
# nothing acts on until the next reboot.
CONFIG_BLOCK_LEGACY_AUTOLOAD=y

# --- parents the shared floor needs -----------------------------------------
# Everything in this block is here for one reason: the shared floor names a
# symbol that arm64's `defconfig` leaves nothing to select. None of it is a
# product requirement -- each is what THIS BOARD'S STARTING POINT happens to
# leave off -- which is why it lives here and not in boards/common/, where it
# would be imposed on every future board as a defconfig quirk. x64's fragment
# carries its own version of this block for the identical reason.
#
# CONFIG_NETFILTER_ADVANCED gates a whole submenu and the floor's
# CONFIG_BRIDGE_NETFILTER sits inside it (`depends on NETFILTER_ADVANCED`).
# With the parent off, olddefconfig does not report the child as `is not set` --
# it drops the symbol entirely, which is how a required option goes missing
# with no line to point at.
CONFIG_NETFILTER=y
CONFIG_NETFILTER_ADVANCED=y

# CONFIG_TRACING, which the floor requires so that fs/tracefs is built -- it
# says at its own line that tracefs has no Kconfig symbol of its own and that
# the entry is an assertion. TRACING is not user-settable: it is `select`ed by
# GENERIC_TRACER, which the event sources below select in turn.
#
# MEASURED, not assumed. arm64 `defconfig` resolves to
# `# CONFIG_FTRACE is not set` -- the entire menu is off, so nothing selects
# TRACING and the floor's line went missing with no `is not set` to point at,
# exactly the failure mode the netfilter note above describes. x86_64_defconfig
# turns FTRACE on together with the kprobe, uprobe and blktrace event sources,
# which is why x64 holds the floor without naming any of this.
#
# The two named here are the smallest set that reproduces x64's outcome:
# FTRACE opens the menu, and BLK_DEV_IO_TRACE selects GENERIC_TRACER, which
# selects TRACING. The kprobe/uprobe event sources x64 also happens to get are
# NOT added -- they are not needed for tracefs and this board has no reason to
# carry them just because the other board's defconfig does.
CONFIG_FTRACE=y
CONFIG_BLK_DEV_IO_TRACE=y

# --- crypto the boot path uses ----------------------------------------------
# dm-verity's hash algorithm is sha256 (rootfs/build.sh writes it into the
# table as HASH_ALGO), and it is asked for through the crypto API before any
# module could be loaded.
CONFIG_CRYPTO=y
CONFIG_CRYPTO_SHA256=y
CONFIG_CRYPTO_LIB_SHA256=y

# --- the board half of the disk-encryption capability ------------------------
# boards/common/mos-required.fragment carries the generic xts(aes) the crypt
# target resolves through, and says at length that it is a capability with no
# consumer in the image. This is the ARMv8 Crypto Extensions AES that the same
# resolution prefers when it is present, and it is the exact counterpart of
# x64's CONFIG_CRYPTO_AES_NI_INTEL -- neither exists on the other architecture,
# which is why neither can live in the shared floor. cx3576 carries this same
# symbol.
#
# NOT `CONFIG_ARM64_CRYPTO`: that umbrella symbol is gone from this kernel
# generation -- it appears nowhere in cx3576's resolved 6.12 config -- so
# naming it here would fail this file's own `=y` assertion on a line that is
# simply obsolete.
CONFIG_CRYPTO_AES_ARM64_CE_BLK=y

# --- what virt-arm64 deliberately does NOT have ------------------------------
# No wireless: boards/virt-arm64/board.env declares BOARD_RADIOS="" and the
# image ships no bluez, no wpasupplicant and no hostapd, so a kernel with a
# WiFi stack would carry drivers for a userland that cannot drive them.
# No sound, no DRM/GPU, no media, no industrial I/O, and no vendor SoC support:
# nothing in the image opens any of them and the machine has none. Each is left
# at whatever arm64 `defconfig` says rather than being switched off line by
# line -- the recorded config/virt-arm64.config is where the resulting answer
# is reviewed, and a hundred `is not set` lines here would be a second copy of
# it.
