{
  "schemaVersion": 2,
  "board": "virt-arm64",
  "revision": "all",
  "bootAssurance": "I1",
  "qualification": "Current signed file deployments: required signed root/support verity, authenticated metadata and component installation. Disposable Secure Boot QEMU enrollment, full runtime/API, component updates and trial fallback pass. This does not qualify physical firmware or hardware-rooted debug policy. Development signing inputs only; conservative I1 board grade retained pending physical/operational qualification.",
  "evidenceRefs": [
    {
      "class": "verity-root",
      "ref": "verify/run.sh --verify --board virt-arm64 --image IMAGE --public-key KEY; current signed file-image checks"
    },
    {
      "class": "ab-fallback",
      "ref": "tests/file-ab-x64/updates.sh and tests/file-ab-x64/faults.sh"
    },
    {
      "class": "update-negative",
      "ref": "pkgs/mos-deploy/tests/deployments.rs and pkgs/mos-deploy/tests/acquisition.rs"
    },
    {
      "class": "signature-negative",
      "ref": "tests/file-ab-x64/trust-rotation.ts and tests/signed-boot-lab/uefi-uki.sh"
    }
  ],
  "physicalBoundaries": {
    "jtag": "No physical debug interface is modeled by this virtual board.",
    "serialConsole": "QEMU console carries boot evidence; product login policy remains unchanged.",
    "recoveryPath": "The operator replaces the complete virtual disk image. Disposable AAVMF enrollment is test state."
  }
}
