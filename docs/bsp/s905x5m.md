# Board dossier: s905x5m

## Identity

X88 Pro X5M / BM201, Amlogic S905X5M (S7D), `MOS_ARCH=arm64`.
The board implementation is in `boards/s905x5m/`.

## Provenance

This mainline adaptation imports the board-owned implementation from
`board/s905x5m` at `e53c1cfd`. The final integration baseline is mainline `09c384cd`;
[PLAN-922](../plan/PLAN-922.md) records the contract changes.

- Kernel: hardkernel/linux at `9ea2aa831a585955dcb0e38a0bc3c63d2c690adc`,
  with its pinned common_drivers submodule, thirteen local patches, board DT
  and Seekwave sources. Input class: source plus firmware blobs.
- U-Boot: CoreELEC/u-boot at `5f7ac2b1dc4df2f466ed88c3958a8675873f8a1a`,
  with the local patch series, including installer receipt patches 0017/0018.
  Input class: source plus vendor boot-chain blobs.
- Seekwave userland: board-local vendor library and integration bridge,
  imported with the source branch; provenance and license notices remain in
  `boards/s905x5m/bsp/userland/`.
- Amlogic package blobs: BM201 recovery inputs from s905x5m-alpine
  `49618bed07508b77bb8e8e640e0faafc5c95ea9a`; the package blob README and
  SHA256SUMS record their exact bytes.

## Supported revisions

The inherited hardware evidence applies to the BM201 units described in
[RFCT-941](../task/RFCT-941.md) and its linked records. No additional PCB,
RAM, storage or alternate-radio SKU is qualified by this adaptation.

## Owners

Repository owner: miehq. Hardware qualification and release acceptance remain
with the board owner; `BOARD_RELEASE_TARGET=0` excludes this port from the
qualified release target set.

## Boot chain

BootROM loads the Amlogic vendor stages and patched U-Boot. U-Boot executes
the board boot script, selects the RAUC A/B slot, loads the kernel and DTB,
and supplies the dm-verity command line. The kernel mounts the immutable
root and systemd mounts the persistent storage tiers.

Redundant U-Boot environments occupy 64 KiB each at 120 and 124 MiB.
The bootloader lives outside the eMMC user-area GPT, in the hardware boot
area. It must not be modeled as cx3576's GPT raw-loader partition.
The inherited vendor signatures do not establish a MOS-controlled verified
chain from BootROM through the root filesystem.
The CX3576 boot-digest protocol uses `mos-boot-digest-a.env` and
`mos-boot-digest-b.env`; the S905X5M boot script does not declare that protocol.
Local main integration preserves this distinction (PLAN-926).

## Storage media and layout

`LAYOUT_VERSION=2` declares twelve partitions in `boards/s905x5m/board.env`:
vendor reserved, vendor environment, two redundant environments, two boot
slots, two verity root slots, META, STATE, EPHEMERAL and DATA.

The SD image formats reserved partition 1 as a cfgload bridge. **The SD image
is not an eMMC whole-disk flashing image.** The dedicated eMMC package and
installer producer retain the different vendor-owned-region contract.
Kernel, U-Boot and userland build exports live under `_out/boards/s905x5m/`;
selected package boot exports live under `_out/s905x5m/boot/`.

## Console

The serial console is `ttyS0` at 921600 baud, with
`earlycon=aml_uart,0xfe07a000`. Confirm the connector and voltage against the
specific board's hardware record before wiring. HDMI output depends on the
vendor display initialization and the board kernel/DT.

## Peripherals

The board declares Wi-Fi and Bluetooth using the Seekwave SWT6621/EA6521QF
firmware and SDIO transport. Bluetooth uses the vHCI bridge, not a UART
attach helper. Its derived address persists in STATE-backed
`/var/lib/bluetooth/bdaddr`; BlueZ uses the hostname plugin for the name.

`BOARD_HWINIT_CONFS` maps audio to ALSA routing, wireless to rail/module
initialization, and Bluetooth to the bridge. The radio packages are selected
independently: declining Wi-Fi retains the shared SDIO transport if Bluetooth
still needs it. Ethernet, HDMI and USB support remain in the kernel/DT and
vendor initialization. No status LED or board recovery action is declared.
RTC retention and the full peripheral matrix need qualification on this image.

The BM201 front panel and MQTT reference application are default-off local
packages selected by `MOS_ROOTFS_COMPONENTS`. The panel reads the current
`/run/mos/timezone` value on each update. The reference application is refused
in production profiles and requires the MQTT feature packages.

## Recovery method

The SD image provides a removable boot path. The separate Amlogic eMMC
package and installer-card producers are documented in
`boards/s905x5m/bsp/uboot/README.md`. Installer selection and its persistent
receipt follow the inherited board state machine. Generating an installer
artifact does not qualify interruption recovery on a device.

No OS reset/recovery action is advertised by `BOARD_RECOVERY_ACTIONS`.
Power-cut recovery and A/B rollback must be re-run on the newly composed
image before release acceptance.

## Artifact digests

The two source-tree commit pins are recorded above. Package blobs carry
`boards/s905x5m/bsp/uboot/package/blobs/SHA256SUMS`; bootloader toolchain
archives and inputs are pinned in their Dockerfiles. Firmware and local
source changes are bound to the integration commit. Output-specific hashes
belong to the generated image/package manifests, not this dossier.

## Known limitations

Opaque Amlogic boot stages and radio firmware reduce auditability. No new
hardware qualification is implied by the inherited branch's historical
passes. The mainline configuration model uses documents under `/mos/config`
and `time.ntp.servers`; it does not migrate the old monolithic settings
file. Existing-device state compatibility requires a separate migration
procedure and validation.

The first mainline SD hardware inspection found the selected front-panel
entry scripts installed as mode 0644. Their unit fails with 203/EXEC and the
health gate consequently refuses to confirm the boot. See RFCT-943 for the
observations and RFCT-944 for the package repair and successful temporary
service probe. The repaired package restores both entry points to mode 0755;
the running SD root has not been replaced. The same run observed an
unsuccessful generated EFI automount and a missing DT model property.

With this layout on both media, PARTUUID links can resolve to different
disks: RFCT-944 observed UENV-A on eMMC and UENV-B on SD. The actual U-Boot
environment belongs to eMMC, and the SD cfgload bridge is not the production
eMMC A/B path. Slot confirmation and OTA are unqualified in this configuration.

## Assurance level

I1: verity image generation and packaged-root checks passed on the captured
mainline-based image.

> status: board-dependent — evidence: `build/src/mkimage-s905x5m-sd.ts`

I2: RAUC bundle production and signed readback passed through the shared
signing path. Installation and rollback on physical hardware remain unqualified.

> status: board-dependent — evidence: `build/src/bundle.ts`

I3: no evidenced MOS-controlled authenticated boot chain.

> status: unsupported

I4: no evidenced hardware-rooted key/rollback policy.

> status: unsupported

## Qualification results

Scope: BM201 / S905X5M, inherited Seekwave radio; actual PCB revision and
storage model must be bound to each hardware run. All rows below refer to
the newly adapted mainline image, not historical branch images.

Host-side evidence is recorded in [RFCT-942](../task/RFCT-942.md): the kernel,
U-Boot and userland builds, 982 build tests, 1,391 verifier tests, 19 selected
local package inspections, 392 SD image checks, and 14 executable smoke checks
passed. These checks used the dev profile with both optional components.

RFCT-943 adds the 2026-09-08 SD hardware run: hostname `mos-490fab24`, SR64G
59.5-GiB SD as `mmcblk1`, and AT3SFA 14.6-GiB eMMC present as `mmcblk0`.
The root and persistent storage are on SD, and both boot-partition hashes
match the named built SD image. MQTT publication and transient container
network/interactive execution passed. The runtime remains degraded because
the health unit remains failed. RFCT-944 subsequently restored the panel
service using temporary binds of the rebuilt package entry points; this
does not qualify a cold boot into the replacement image.

| Case | Result | Date | Evidence |
| --- | --- | --- | --- |
| Installation and first boot (dev profile) | fail | 2026-09-08 | RFCT-943; SD boots but health fails; eMMC installation remains unqualified |
| Cold boot and reboot | not tested | — | Hardware run required |
| A/B update and rollback | not tested | — | Hardware run required |
| Power interruption during update/installation | not tested | — | Hardware run required |
| Ethernet, Wi-Fi and Bluetooth peer operation | not tested | — | Full peer matrix untested; narrower RFCT-943 checks are listed below |
| HDMI, audio and USB | not tested | — | Visible/audible output and input events unconfirmed; narrower checks below |
| Front panel and timezone changes | not tested | 2026-09-08 | RFCT-944; temporary service repair passed, but replacement-image boot and timezone changes remain untested |
| Front-panel service start, stop and restart | pass | 2026-09-08 | RFCT-944; corrected package scripts temporarily bound into the existing SD boot |
| Thermal, storage endurance and long-duration operation | not tested | — | Hardware run required |
| Ethernet link, DNS and TCP egress | pass | 2026-09-08 | RFCT-943; 1000/full and successful container HTTP egress |
| Wi-Fi and Bluetooth discovery | pass | 2026-09-08 | RFCT-943; scan only, no association or pairing |
| Managed Wi-Fi association, DHCP, DNS and HTTPS | pass | 2026-09-08 | RFCT-945; 5 GHz WPA2, wlan0 address 192.168.27.73, link-specific DNS and link-bound HTTPS; native settings toggle used because the browser API was refused |
| Wi-Fi gateway ICMP | fail | 2026-09-08 | RFCT-945; four unanswered probes on wlan0, while the Ethernet control passed; cause unresolved |
| MQTT reference publication | pass | 2026-09-08 | RFCT-943; 9 items, heartbeat and one completion |
| Container default networking and interactive runtime | pass | 2026-09-08 | RFCT-943; setting restored after the transient probes |
| HDMI PCM access and USB enumeration | pass | 2026-09-08 | RFCT-943; one second of silence and keyboard/mouse receiver enumeration |
