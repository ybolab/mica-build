# 20260910-0040-strict-file-ab Strict two-version file A/B installation

- **status**: implementing
- **createdAt**: 2026-09-10 00:40
- **approvedAt**: 2026-09-10 00:40 (explicit user instruction)
- **relatedTask**: [20260910-0040-strict-file-ab](../task/20260910-0040-strict-file-ab.md)

## Contract

DATA `/mos/updates` owns download, verification and acquisition state. The running
confirmed deployment A stays bootable throughout installation. After all new
inputs are authenticated and replacement capacity is checked, durably retire old
B, reconcile DATA references, reclaim B-exclusive components, and publish new B.
Shared A/new-B objects survive collection. Publish the new boot entry only after
all objects and descriptors are durable. An interrupted replacement boots A;
a complete candidate uses three trial attempts, falling back to A on failure.
There are at most two deployment records, independently of the trial count.

## Steps and acceptance

1. Add failing transaction regressions for retirement, shared-component reuse,
   confirmed-running enforcement and crash recovery on both backends.
2. Implement ordered retirement and reclamation with complete source validation;
   verify each injected write/fsync/rename failure preserves a usable A.
3. Set two-record bounds and two-version capacity in builders and firmware;
   verify parser rejection, realistic filesystem space gates and factory tests.
4. Update current architecture/update/storage documentation, run focused review
   and relevant Rust, build, firmware and virtual boot/update acceptance.
5. Commit the scoped change and record source/artifact provenance and any
   physical acceptance still requiring bench hardware.

## Constraints

Existing unrelated cx3576 boot-log planning edits remain untouched. No changes to
camera, display, accelerator or OP-TEE configuration belong to this task. Preserve
1 GiB cx3576 SYSTEM geometry and timestamped complete factory image filenames.
