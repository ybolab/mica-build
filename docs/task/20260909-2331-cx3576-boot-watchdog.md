# 20260909-2331-cx3576-boot-watchdog Repair the cx3576 firmware watchdog boot blocker

- **status**: in_progress
- **priority**: P0
- **owner**: worker/cx-watchdog-20260909
- **createdAt**: 2026-09-09 23:31
- **relatedPlan**: [20260909-2331-cx3576-boot-watchdog](../plan/20260909-2331-cx3576-boot-watchdog.md)

## Description

The physical cx3576 reports `MOS FIT recovery: required boot watchdog unavailable`
before loading Linux. Diagnose the shipped configuration and control device tree,
repair the mandatory watchdog integration and rebuild a complete current image.
The existing P10 implementation and full-image delivery authorization applies.

## ActiveForm

The complete repaired image passes software acceptance; awaiting physical startup,
watchdog handoff/reset and recovery-session retest on the cx3576 bench.

## Verification

Reproduce the missing watchdog integration on the shipped artifact, establish
regression coverage, verify the rebuilt firmware and complete image, and retain
physical startup and watchdog reset as explicit bench acceptance.

## Findings

The shipped control FDT has an enabled `/soc/watchdog@2ace0000` with tclk/pclk.
The RK3576 clock provider has no enable callback; non-CCF `clk_enable` returns
-ENOSYS, and DesignWare probe propagates it. This reproduces as -38 using the
actual pinned DesignWare source and clock ops. The new RK3576 callback opens
CRU_CLKGATE_CON16 bits 7 and 8 before watchdog MMIO access. Unknown clock IDs
retain their prior unsupported result.

The regression passes with the patch, including probe, clock gates, arm/feed
registers and unrelated-clock refusal. Actual firmware policy fixtures also
verify that probe/start errors enter recovery before media reads or trial writes.
The build now runs this driver regression and checks the shipped control FDT.
No watchdog requirement, signature policy or compatibility path is relaxed.

Evidence: `.tmp/cx-watchdog/red.log` (probe -38, exit 134), `green.log` (exit 0).
The initial test compilation exposed the upstream unused `flags` parameter;
its warning handling is aligned with the upstream build before establishing RED.
Clock gates are cross-checked against the official RK3576 Linux clock driver:
https://raw.githubusercontent.com/rockchip-linux/kernel/develop-6.1/drivers/clk/rockchip/clk-rk3576.c

The enabled watchdog also exposed an idle recovery defect: the pinned RockUSB
loop never called `schedule()`. With Ctrl-C disabled and no USB interrupts, no
cyclic watchdog service ran. The new patch calls the scheduler on every loop.
The fixture extracts the actual pinned RockUSB loop and reproduces 10 idle polls
with zero services before the patch, then 10 services after it. This preserves
the watchdog during long recovery sessions instead of disabling it.

Recovery evidence: `.tmp/cx-watchdog/recovery-red.log` (exit 134) and
`recovery-green.log` (exit 0, driver, idle recovery and firmware I/O checks).
The preliminary image passed 123 offline checks but is superseded by the full
rebuild that includes the recovery fix. Physical startup remains unverified.

## Delivery

The repaired complete image is `_out/cx3576/image/mos-cx3576-20260909-235323.img`
(2,435,842,048 bytes), SHA-256 `d8c29cd014ba574bee3c36c90adb6f44d691dd92e20da1480746a0770c17c04f`.
Firmware and assembly use clean `d70a9b26aa4f`; authenticated kernel and root
components retain their clean `38f2a37b9a3c` identity. The separate component
sources and validation are recorded in `_out/cx3576-watchdog-r2/build-record.json`.
The signed firmware package is `_out/cx3576-watchdog-r2/firmware`.

The actual BSP build runs both pinned-source regressions and passes its mandatory
configuration, control-FDT and size checks. FIT tests accept the required key and
reject missing signatures, unknown keys and modified kernel/DTB/initramfs. The
complete image passes 123 offline checks without skips, including exact loader
readback against its authenticated receipt. Flash geometry and SHA-256 pass.
Logs: `.tmp/cx-watchdog/final-build/`, controller `final-build.rc` is zero, and
image-verifier evidence `_out/verify/cx3576-6u47cG`.

The earlier partial fix image is retained only under `_out/cx3576-watchdog/image`
and removed from the default flash directory. SYSTEM geometry is unchanged.
The previous release directory still identifies the earlier firmware and is not
the delivery for this repair; no new release provenance is claimed for reused
components. Local review covers gate masks, mandatory failure ordering, cyclic
service, actual-source fixtures and the assembled firmware receipt. No remaining
blocking software finding was identified in that scope.

The bench guide and collector no longer claim that SYSFS/NOWAYOUT are disabled;
the delivered kernel enables both. Bash syntax, toolchain lint, documentation
checks and staged whitespace checks pass. No local board endpoint was supplied,
so actual startup, a long RockUSB recovery session, watchdog reset and Linux
handoff still require physical evidence. P10 remains open.

The latest full-image retest target is now
`_out/cx3576/image/mos-cx3576-20260910-001906.img`, which includes both watchdog repairs
and the [1 GiB SYSTEM change](20260909-2358-cx3576-system-1g.md). The earlier images above
remain historical evidence; use the latest full image for the physical retest.
