# 20260909-2358-cx3576-system-1g Reduce cx3576 SYSTEM to 1 GiB

- **status**: completed
- **priority**: P1
- **owner**: worker/cx-system-1g
- **createdAt**: 2026-09-09 23:58

## Description

The user explicitly requests a 1 GiB SYSTEM partition. Update the current cx3576
factory geometry, firmware validation and flash readback ranges, then deliver a
complete current image. No old-layout support or migration is allowed.

## ActiveForm

The 1 GiB SYSTEM image is delivered with software acceptance complete; physical
startup remains in the separate P10 bench task.

## Verification

Accept 1024 MiB SYSTEM and last-growing DATA in firmware, render matching runtime
repart limits, reject an inconsistent layout before storage mutation, and verify
a rebuilt 1299 MiB factory image with real DATA growth and FIT signature checks.
Physical cx3576 startup remains part of the open P10 bench acceptance.

## Findings

RED is established for the new board geometry (layout suite exits 1), the actual
firmware policy (exit 134 before any candidate write), and flash preflight (exit 1,
wrong factory image size). Logs are under `.tmp/cx-system-1g/red-*.log`.
The fixed runtime repart policy is generated by the board package, so it is
rebuilt with the ARM64 pool and root instead of reusing the previous signed root.

GREEN passes all 384 build tests, the actual firmware policy fixture (including
refusal of a 2 GiB SYSTEM and acceptance of grown DATA), and complete flash
readback ordering with corrupt firmware/SYSTEM/DATA bytes refusing reset. The
rendered SYSTEM policy fixes both bounds at 1,073,741,824 bytes. Documentation,
shell syntax and host-toolchain checks pass. Local review finds no blocking issue
in the geometry consumers, readback lengths and packaged repart derivation.

## Delivery and acceptance

- Complete image: `_out/cx3576/image/mos-cx3576-20260910-001906.img`, 1,362,100,224 bytes
  (1299 MiB), exactly 1 GiB smaller than the prior factory image.
- SHA-256: `fbe52a42a788fbfc26035ee016ceca0eb369c8f7acb72fb385a43556c77988e9`.
- SYSTEM: 1,073,741,824 bytes; allocated blocks use 197.020 MiB including
  filesystem overhead, with 826.980 MiB free. File payload is 149.990 MiB.
  Three full payloads plus the 128 MiB reserve require 577.969 MiB.
- Firmware, root and assembly: clean `97bb466792ca`; the independently signed
  kernel/support retains clean `38f2a37b9a3c`. Exact component sources and size
  measurements are in `_out/cx3576-system-1g/build-record.json`.
- Every ARM64 pool producer is rebuilt at one source stamp. The signed root's
  actual `/etc/repart.d/20-system.conf` matches the new 1 GiB min/max limits.
- All 384 build tests, actual firmware I/O/layout fixtures, complete flash
  readback ordering/corruption checks, required FIT signature negatives, all
  123 offline checks without skips and independent GPT/loader/counter inspection
  pass. Image evidence: `_out/verify/cx3576-MSf39H`.
- Real DATA growth using the packaged policy passes at `_out/data-growth.7egNCF`:
  every firmware/counter/SYSTEM byte and protected partition boundary is unchanged;
  wrong SYSTEM and disk identities are refused before GPT mutation.
- The root smoke run has 11 passes and one explicit crun executor limitation:
  qemu-user cannot perform its memory-file-descriptor re-execution. This is not
  counted as crun acceptance and remains a native-device check.

The first growth attempt stopped before attaching the loop disk with
`mknod: invalid minor device number '0 (lost)'`. A read-only diagnostic then
observed a normal `/dev/loop0`; the unchanged test passed on a fresh copy.
The failed attempt remains in `.tmp/cx-system-1g/growth-initial.log` and
`_out/data-growth.tEcG55`. The build's initial nonzero result is retained;
`resume.rc` is zero and records the successful growth, inspection and image
checksum. Logs are under `.tmp/cx-system-1g/`.

Local review finds no blocking software issue in this change. No compatibility
or migration path is present. The completed focused plan `20260909-2358-cx3576-system-1g` is
consolidated into this task and the current board/layout documents. Physical
cx3576 boot, watchdog/recovery behavior and power-cut qualification remain open
in the original P10 delivery and watchdog tasks.

- complete: The complete 1 GiB SYSTEM image and focused software acceptance are delivered; P10 physical acceptance remains separate.
