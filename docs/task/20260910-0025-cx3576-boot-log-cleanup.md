# 20260910-0025-cx3576-boot-log-cleanup Resolve cx3576 boot configuration mismatches and verify runtime handoff

- **status**: in_progress
- **priority**: P1
- **owner**: worker/cx3576-boot-cleanup-20260910
- **createdAt**: 2026-09-10 00:25
- **relatedPlan**: [20260910-0029-cx3576-boot-log-cleanup](../plan/20260910-0029-cx3576-boot-log-cleanup.md)

## Description

Prepare a complete repair plan from `_out/cx3576/image/a.txt`, with the user's
confirmation that HDMI was disconnected. Cover board configuration mismatches,
FIT descriptions, initramfs memory accounting, and current-image acceptance.
Development-stage changes do not require compatibility with previous layouts.
The current request authorizes investigation and the written plan; implementation
of the full repair remains subject to plan approval.

## ActiveForm

Investigation complete; the repair and acceptance plan is drafted for review.

## Dependencies

- **blocked by**: implementation approval and board access for physical acceptance
- **blocks**: complete cx3576 runtime acceptance
- Coordinate with the existing watchdog and signed-deployment delivery tasks;
  their ownership and status remain with their current owners.

## Notes

The historical log predates the completed 1 GiB SYSTEM change. The currently
packaged initramfs has the same SHA-256 as the log, allowing exact archive
accounting without treating the old disk log as current-layout evidence.

The exact archive matches the logged hash. Its exitrd file payload is 17,253,688
bytes; the original 33,615,360-byte archive is released during kernel startup.
HDMI disconnection is an expected-state annotation, not a display failure.

Planning validation: `make docs-verify` passed all five checks. Direct validation
also passed for the new task/plan links, index entries, state markers, and pending
approval. `git diff --check` passed. No implementation or hardware test was run.
