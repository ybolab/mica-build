# 20260910-0040-strict-file-ab Replace the inactive deployment within a strict two-version budget

- **status**: completed
- **priority**: P1
- **owner**: worker/strict-ab-20260910
- **createdAt**: 2026-09-10 00:40

## Description

The user approved downloading and authenticating updates in DATA, retiring the
inactive deployment only while the running deployment is confirmed healthy, and
publishing a complete replacement as the second bootable deployment. SYSTEM
holds at most two deployments and their authenticated component metadata. No
third-version capacity allowance, old-format compatibility or migration path.

## ActiveForm

Implementation, native fault acceptance, x64/ARM64 virtual boot/update acceptance
and the cx3576 image delivery are complete at source `0209c4bf462b`. Physical
cx3576 startup remains with the parent plan's P10 hardware acceptance.

## Verification

Exercise both UEFI and FIT transactions with interruption and I/O faults, shared
components, invalid inputs, and insufficient space. Check two-record bounds in
firmware and builders, run the Rust/build gates and virtual boot/update checks.
Keep physical cx3576 acceptance explicitly separate until a device is available.

## Implementation and focused evidence

The old transaction reproduces a third boot record in the new signed integration
fixture. The revised transaction proves confirmed-running identity, authenticates
all inputs, preflights reclaimable allocated blocks, retires old B on both native
backends and preserves A/new-B shared objects. Both FIT environment copies forget
retired records before collection; restart reconciliation drops stale DATA
fallback references. Publication and three-attempt activation remain ordered.

The Rust gate passes 46 tests (two separately executed acceptance tests are
intentionally excluded by nextest). The bounded tmpfs test passes both the
replacement-only capacity case and refusal before retirement when capacity is
still insufficient. Native fault acceptance passes all 549 interrupted/error
cases across 183 actual I/O boundaries, followed by transaction restart and
validation of each valid redundant FIT copy. Evidence: `_out/io-faults.AQr4V5`.
The clean-source full build suite passes 385 tests. Firmware parser/I/O fixtures and docs
verification pass. Logs are under `.tmp/strict-ab/`.

The incomplete synthetic install fixtures were replaced with signed current,
inactive and incoming deployments on both backends. Current/fallback swapping,
unlaunched-trial refusal, generation recovery and component status remain covered.
The runtime acquisition fixture now tests the earlier retirement-state failure
boundary and retries after DATA repair; each successful virtual installation must
leave exactly two descriptors and native entries.

Local review uses the Rust and TypeScript backend packs. No compatibility path
is introduced. New physical startup evidence remains pending and is not inferred
from native transaction tests or prior image logs.

Factory capacity now also uses the actual formatted ext4 header: fixed metadata,
superblock reserved blocks and the kernel's internal reserve are subtracted before
requiring two complete payloads plus the 128 MiB installation reserve. A real
1 GiB filesystem accepts 350 MiB per payload and rejects 420 MiB, even though the
latter passes the raw-partition check. The first new tool test timed out after
60,000 ms; it passes in 3.99 seconds after using the existing project-wide
300-second tool-test ceiling. The original failed log is retained; the focused
seven-test layout/environment run and the subsequent clean-source full suite pass.

## Virtual acceptance and production rebuilds

The x64 guest passes signed startup, shutdown, root-only and kernel-only updates,
confirmed-health failure recovery, three failed candidate trials with fallback,
and a combined update. A further real archive import through DATA `/mos/updates`
passes the retirement-state write-failure/restart/retry case and boots the new
candidate successfully. Every installation leaves exactly two descriptors and
native entries. Evidence: `.tmp/strict-ab-checkout/_out/file-runtime.yuWDYU/boot`.

The same complete sequence also passes on virt-arm64, including confirmed-health
failure, exactly three failed trials, fallback identity, combined update, real
DATA archive import, retirement-state write-failure repair/retry and new candidate
startup/shutdown. All five installations on each architecture pass the two-record
bound. ARM64 evidence: `.tmp/strict-ab-checkout/_out/file-runtime.mXNi8a/boot`.

Current native packages, init and production roots have been rebuilt from the
clean implementation checkout for x64, virt-arm64 and cx3576. The x64 root passes
all 12 registered artifact smoke checks. Both ARM64 roots pass 11 checks with
the existing crun qemu-user fexecve executor limitation explicitly unverified.
The pinned x64 and virt-arm64 Linux 6.12.107 kernels were rebuilt with the current
content trust certificate. The cx3576 BSP kernel is reused from the recorded
`38f2a37b9a3c` artifact; its FIT is rebuilt with current init and signatures.

The initial x64 Debian snapshot download stalled and its build was stopped
(exit 143, log retained). Prefetching the exact locked packages from the official
Debian pool/snapshot mirror verified all 163 cache entries and resumed the clean
build successfully without changing dependency pins.

## cx3576 image verification

The timestamped factory image `mos-cx3576-20260910-015700.img` is 1,362,100,224
bytes (1,299 MiB), with a 1 GiB SYSTEM partition. All 123 offline image checks,
flash geometry, real DATA-only growth, required FIT signatures and tamper/missing/
unknown-key rejection pass. The growth test verifies firmware, both native
environment regions and SYSTEM remain byte-identical. Evidence:
`.tmp/strict-ab-checkout/_out/verify/cx3576-FH7Imw` and
`.tmp/strict-ab-checkout/_out/data-growth.jL6qtT`.

The factory pair shares its initial root/kernel objects: logical payload is
157,280,036 bytes (149.994 MiB), while ext4 allocated blocks including metadata
occupy 206,585,856 bytes (197.016 MiB). After filesystem overhead/reserves and the
128 MiB installation reserve, the two-deployment payload budget is 816,021,504
bytes (778.219 MiB), or 408,010,752 bytes (389.109 MiB) each when divided equally.
These are shared capacity limits, not fixed slot boundaries.

The parent plan's third-generation budget and SYSTEM acquisition directory were
removed to match the approved replacement contract. Host USB/serial enumeration
found no connected bench device; physical startup and the parent P10 hardware
acceptance remain open pending a device endpoint.

## Delivered artifact

- Image: `_out/cx3576-strict-ab/image/mos-cx3576-20260910-015700.img`.
- SHA-256: `86f6262a78854690c63f446880390c782ca443fc2221c4048c70a25e83d842af`.
- Source: clean `0209c4bf462ba270fdc24bc58d7f791629a58ca6` for native packages,
  root, init, U-Boot firmware and FIT/image assembly; the reused BSP kernel is
  identified above. This is the pinned strict A/B acceptance snapshot.
- `build-record.json`, `system-capacity.json`, public metadata key, component
  manifests and selected verification logs accompany the image. `SHA256SUMS`
  covers all delivery files and passes its final check. No private keys are in
  the delivery directory.
- The completed focused plan is consolidated into this task and the updated
  parent plan; current behavior remains in `docs/design/updates.md` and
  `docs/design/storage.md`.

- complete: Implemented and delivered with x64/ARM64 virtual and cx3576 offline acceptance; physical P10 remains tracked by the parent task.

## Workspace handoff (2026-09-10 02:49 UTC)

The user requested a complete handoff after asking that other repairs remain
separate. The observed branch is `main`, with implementation `0209c4bf462b` and
acceptance/documentation `06be7fa5`. The index is empty; the working tree contains
the separate repairs listed below. This handoff records their state and does not
claim their changes, commit them, or change their owners.

### Required behavior and completed work

- Development uses complete latest factory images. Do not add old-format
  compatibility, migration, dual installers or fallback readers.
- Acquisition and authentication belong on DATA `/mos/updates`. Confirm running
  A, preflight replacement capacity, retire old B, reclaim B-exclusive objects,
  publish new B durably, and activate its native entry last. Both redundant FIT
  copies forget B before collection. Keep at most two deployment records and
  three trial attempts; preserve objects shared with A or incoming B.
- cx3576 SYSTEM stays 1 GiB. Timestamped image filenames identify builds.
  Logical A/B deployments share immutable component objects; the measured
  capacity and current occupancy are recorded above.
- RAUC is removed. lode is not an OS update dependency. Current contracts are
  [updates](../design/updates.md), [storage](../design/storage.md) and the
  [parent plan](../plan/20260908-1428-file-ab-signed-components.md).
- The earlier [apid reboot repair](20260909-1421-apid-reboot.md) has software and
  x64 actual-reboot evidence. Its reported physical-device diagnosis remains
  open. Earlier `_out` evidence removed by the requested clean rebuild is
  historical; retained controller logs are under `.tmp/reboot-investigation/`.
- All strict A/B acceptance exit codes are zero. This handoff adds no new
  runtime acceptance claim. The 46 Rust tests, 385 build tests, 549 native fault
  cases, complete x64/ARM64 sequences and 123 cx3576 offline checks above are the
  current strict A/B evidence.

### Other repairs left in the working tree

| Work | Recorded owner and state | Scope and evidence |
|---|---|---|
| [Centered HDMI logo](20260910-0044-cx3576-hdmi-fullscreen-logo.md) | `display/session-20260910-0044`; completed, uncommitted | `boards/cx3576/board.env`, kernel config, `build/src/kernel-package.ts`, new `build/src/kernel-display.test.ts`; one centered logo and hidden VT cursor. The owning task records four regressions, 388 build tests and image checks. |
| [U-Boot console](20260910-0159-cx3576-uboot-console.md) | `boot-console/session-20260910-0159`; completed, uncommitted | `build-mos.sh`, `mos-file-boot.c`, signed-file-boot patch, new `uboot/tests/autoboot.{c,sh}`, `tests/file-ab-fit/firmware-io.c`, and console design documentation. Restores the native one-second any-key countdown and `bootcmd=mosboot`. The owning task records source/firmware tests and image checks. |
| [Boot-log cleanup](20260910-0025-cx3576-boot-log-cleanup.md) | `worker/cx3576-boot-cleanup-20260910`; in progress, plan drafted | Historical boot-log investigation and archive accounting are complete; the full repair plan records implementation approval and board access as outstanding. |
| [Late HDMI attachment](20260910-0117-cx3576-late-hdmi-logo.md) | Unassigned; pending | Separate logo-lifetime/hotplug limitation; the centered-logo fix does not implement it. Review the display contract and D6 bench row before choosing a repair. |

Their task/plan detail files are untracked; their entries share the modified
`docs/changelog.md`, `docs/task/index.md` and `docs/plan/index.md`. Preserve those
edits when staging any later commit. Completed task status does not mean that
its source is committed or its physical behavior has been tested.

The console task also records an unresolved existing shell-lint finding:
`FAIL: boards/cx3576/bsp/uboot/embed-trust.sh:15: an early-exiting grep on the right of a pipe, in a file that sets pipefail: the pipeline reports failure when the pattern IS found. Use 'grep -c ... >/dev/null'`.
Its scoped review passed; this is not evidence that the whole-tree shell gate
passes. The finding was not repaired during this handoff.

### Image selection and integration gap

All three raw images are 1,362,100,224 bytes. The HDMI candidate is currently
stored only as a 110,095,232-byte gzip archive. Their independent build records
are the source of truth for component provenance; filename order cannot establish
that an image contains every repair.

| Candidate | Image under `/srv/mos/` | Component boundary |
|---|---|---|
| Strict A/B | `_out/cx3576-strict-ab/image/mos-cx3576-20260910-015700.img` | Clean `0209c4bf462b` root/init/firmware/assembly; BSP kernel from `38f2a37b9a3c`, freshly signed FIT. Does not include the separate HDMI/console working-tree repairs. |
| HDMI | `_out/cx3576-hdmi-20260910/image/mos-cx3576-20260910-011849.img.gz` | Rebuilt HDMI kernel; root/firmware reused from the earlier 1 GiB image. |
| Console plus HDMI | `_out/cx3576-uboot-console-20260910/image/mos-cx3576-20260910-020836.img` | New console firmware plus the HDMI kernel; still reuses the earlier root. |

The latter two reuse root version `20260910-000311-97bb466792ca`, ID
`2980b3b724e2c0a0d046347e6f51ed376a248670f722a0798725feee50ff7b31`.
The strict A/B image uses root version `20260910-012122-0209c4bf462b`, ID
`f43688c5c345c3ef3e570498d6163e135e35e1770a1f5dbbcdb84da6e82b4409`.
No listed image combines current strict A/B userspace with both separate repairs.

The strict A/B SHA-256 is recorded above. HDMI SHA-256 is
`2f4f2fc21e5cd6c19366574b00880b1ab96043c54d08c2c3aa1ac9ca535d3ed7`;
console/HDMI SHA-256 is
`2da1a3dba7429faf6f3b4c6e95ba00cb0f44b6e3f6b1868fe822c748c68317af`.
For the latter two, `build-record.json` is in the respective output root;
for strict A/B it is inside `image/`. Each image directory has `SHA256SUMS`.
The HDMI checksum file still names the raw `.img`: verify the decompressed
stream against that hash, or extract it before using `sha256sum --check`.

The shared `_out/cx3576/image/system.img` is a stale 2 GiB intermediate, even
though the timestamped `mos-cx3576-20260910-001906.img` in that directory uses
1 GiB SYSTEM. Use the component set and intermediate files matching the chosen
build record, not unqualified intermediate filenames from that shared directory.

### Next execution order

1. Recheck git status and the recorded owners before changing their files.
   Review and commit the completed HDMI/console repairs with their own evidence;
   preserve pending boot-log and late-attachment work. Resolve the recorded shell
   gate finding before claiming a complete integrated gate pass.
2. Build one integrated factory image from the final source: current native
   packages/root, current `mos-init`, the corrected HDMI BSP, and the console/
   watchdog firmware. Repack and sign the FIT with current init, keep all package
   producers at one source stamp, and record reused component inputs explicitly.
   Do not promote the older-root console image as the integrated result.
3. Run the affected build/Rust/firmware gates, image signatures and offline
   checks, DATA-only growth and relevant virtual boot/update acceptance. Record
   the new timestamped image, source identities and checksum together.
4. Obtain the cx3576 bench endpoint and flash the complete integrated image.
   Verify cold startup, one-second console interruption without trial consumption,
   connected HDMI output, actual apid reboot, watchdog handoff/reset, RockUSB
   recovery dwell, failed-candidate fallback and power-cut recovery. The host
   USB/serial enumeration found no board and no network endpoint was supplied.
5. Keep [parent delivery](20260908-2229-file-ab-delivery-x64-first.md),
   [watchdog acceptance](20260909-2331-cx3576-boot-watchdog.md) and P10 open until
   their physical requirements are measured. Resume the pending boot-log and
   late-HDMI work through their recorded scope and approval state.

### Local continuation resources

- Repository: `/srv/mos`; clean implementation clone:
  `/srv/mos/.tmp/strict-ab-checkout` at `0209c4bf462b`.
- Strict A/B controllers, logs and exit codes: `.tmp/strict-ab/`. Useful scripts:
  `resume-build.sh`, `cx-resume.sh`, `runtime.sh`, `archive-recovery.sh` and
  `deliver.py`. They pin source and existing output locations; inspect them
  before adapting a new build rather than rerunning into those directories.
- Full virtual guest logs: the x64 and ARM64 paths in the acceptance section
  above. Native fault evidence: `_out/io-faults.AQr4V5`.
- Other repair logs: `.tmp/cx3576-hdmi-20260910/` and
  `.tmp/cx3576-uboot-console-20260910/`.
- The `strict-ab*` windows in tmux session `mos-de3c7c` contain idle shells.
  Strict A/B build/test processes have finished; their containers were removed.
  Recheck live processes before reusing windows or stopping resources. Do not
  sweep other repairs' containers or generated artifacts.
- Development signing material remains in ignored local build inputs. The
  delivered strict A/B directory contains only public material. Preserve local
  inputs for reproduction and never include private keys in a commit or handoff
  bundle.
