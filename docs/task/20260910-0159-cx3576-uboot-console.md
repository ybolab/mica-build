# 20260910-0159-cx3576-uboot-console Restore the standard CX3576 U-Boot console entry

- **status**: completed
- **priority**: P1
- **owner**: boot-console/session-20260910-0159
- **createdAt**: 2026-09-10 01:59
- **relatedPlan**: [20260910-0159-cx3576-uboot-console](../plan/20260910-0159-cx3576-uboot-console.md)

## Description

Restore the standard one-second, any-key U-Boot console entry in the MOS
firmware and document that this development interface must remain available.
Timeout and the console's `boot` command must execute the existing signed FIT
deployment policy. No compatibility path is required.

## ActiveForm

Completed the standard U-Boot console repair and candidate verification

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

- Full tier: firmware configuration, command integration, native regressions,
  build verification, and the requested design explanation.
- The user explicitly authorized documentation and implementation after the
  investigation; no additional approval is pending.
- Commit `1875d1332170a5d4f883c8a430462f3d6d22be20` introduced both
  `BOOTDELAY=-2` and a nonreturning policy call before `cli_init()`. Changing
  the delay alone cannot restore the prompt.
- The pinned native CLI services the cyclic scheduler while waiting for input;
  retain the watchdog and the recovery-key path in the signed boot command.
- Track source/build verification separately from physical UART acceptance.
- RED: the patched old main loop fails `cli_ready && !ctrlc_disabled` before
  CLI initialization. The old policy fails the registered `mosboot` assertion.
- GREEN: native one-second timeout, initial/delayed any-key input, Control-C
  input at 990 ms, and the standard `boot` command pass. The complete registered
  policy passes trial-record, write/flush/readback failure, watchdog failure,
  invalid-layout, corrupt-record, and physical-recovery-key simulations.
- The actual MOS build passes the native autoboot, watchdog-driver, and RockUSB
  watchdog tests and resolves `BOOTDELAY=1`, `BOOTCOMMAND="mosboot"`. Final
  source build and signed image verification have also completed successfully.
- Host-toolchain lint and all five layout tests pass; all five documentation
  gates pass. The unrelated existing shell lint finding remains verbatim:
  `FAIL: boards/cx3576/bsp/uboot/embed-trust.sh:15: an early-exiting grep on the right of a pipe, in a file that sets pipefail: the pipeline reports failure when the pattern IS found. Use 'grep -c ... >/dev/null'`.
  This task's changed shell code has no such finding; the existing trust helper
  remains unchanged.
- Scoped pma-cr review: PASS, zero findings. The existing signed transaction is
  unchanged and is entered only through the registered command. The native
  `boot` command reads `bootcmd`; no persisted command environment is enabled.
  Console input reaches `fgetc()` and its cyclic scheduler, so the standard
  idle prompt retains watchdog servicing. Physical UART behavior is untested.
- Final firmware build passes the configuration, command-symbol, control-FDT,
  watchdog-driver, and native autoboot assertions. The produced loader contains
  `bootcmd=mosboot`, `bootdelay=1`, and the native any-key prompt.
- The required-key check extracted from the actual new loader accepts the HDMI
  kernel's FIT and rejects changed kernel/DTB/initramfs payloads, a missing
  signature, and an unknown key.

## Delivery

- Image: `/srv/mos/_out/cx3576-uboot-console-20260910/image/mos-cx3576-20260910-020836.img`.
- Size: 1,362,100,224 bytes (1,299 MiB).
- SHA-256: `2da1a3dba7429faf6f3b4c6e95ba00cb0f44b6e3f6b1868fe822c748c68317af`.
- Firmware component: `3e795596158e520aea36d7333bb97da1d5ecd4956d313fc68ae9835406f58c94`, generation 4.
- Reuses the verified centered-single-logo, hidden-cursor HDMI kernel and signed
  root from the previous candidate. The new loader bytes match both the signed
  firmware package and the image at disk offset 32768.
- All 123 offline checks pass, zero skipped; flash geometry and SHA256SUMS pass.
- Source and artifact identities: `_out/cx3576-uboot-console-20260910/build-record.json`.
  Logs: `.tmp/cx3576-uboot-console-20260910/`.
- Physical UART and HDMI behavior have not been tested on a board. No device
  was flashed, and no commit or push was made.

- complete: Standard one-second any-key entry restored and documented; rebuilt signed firmware and combined HDMI candidate passed software verification.
