# 20260910-0554-s905x5m-current-system Integrate S905X5M with the current signed-file system

- **status**: completed
- **priority**: P1
- **owner**: worker/s905x5m-20260910-0554
- **createdAt**: 2026-09-10 05:54

## Description

Investigate and complete the S905X5M board against the current MOS architecture
and working changes. Target fresh development images without compatibility
readers, old-layout migration or restoration of the retired RAUC image path.
Determine the native signed boot, packaging, storage and peripheral gaps before
proposing implementation.

## ActiveForm

Completed S905X5M SD development integration and offline acceptance.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

- Full PMA tier: board boot, image/component production and verification span modules.
- Preserve the existing writable-var/regdb work owned by worker/writable-var-20260910.
- Tracking uses repository files and the PMA task-state script.
- Implementation approved by the user on 2026-09-10.

## Investigation and proposal

- [Concrete proposal](../plan/20260910-0559-s905x5m-current-system.md) records the current gaps, delivery
  order, board geometry candidate and per-step acceptance criteria.
- The missing current image target is reproduced by a make dry-run.
- Package resolution separately reproduces the existing `.tmp` duplicate-producer
  failure; the plan records isolation rather than changing unrelated discovery.
- Investigation completed before approval; implementation is now in progress.

- Proposal verification: `make docs-verify` passed (195 index, 498 links,
  718 status, 249 translation coverage and 118 board checks). Task/plan index,
  local links, lifecycle state and whitespace checks passed.

## Implementation evidence

- Implementation started from `5c61f7fbb558`, including the completed writable-var,
  regulatory trust and console changes. The isolated source snapshot excludes the
  existing `.tmp` producer collisions without changing discovery policy.
- Implemented the SD FIRMWARE/SYSTEM/DATA geometry, independent signed boot0
  receipt, native FIT policy, board/radio packages and current component/image CLI.
- U-Boot exports pass the decompressed FIP/BL33/control-key check. The recovery
  package round-trip passed. Actual signed FIT verification rejects modified
  kernel/DTB/initramfs, missing signatures and unknown keys.
- Rust `hack/check.sh` passed fmt, strict clippy, nextest, doctests and cargo-deny.
  The real filesystem transaction gate passed 231/21/15 UEFI and 228/30/24 FIT
  interrupted/error cases across install/confirm/GC boundaries.
- Build and verifier suites passed 394 and 646 tests; the manifest gate passed
  46 checks, 257 resolutions and six refusals. Native C record/IO tests and all
  four independent radio selections passed.
- Added the S905X5M audio dependency closure from the existing authenticated
  `20260905T000000Z` Debian snapshot; no existing package version was upgraded.
  The snapshot InRelease signatures and arm64 index digest were checked before
  adding the pins. Lock and selected-producer preflight checks passed.
- Review found the vendor watchdog's kernel-feed mode reserves the userspace
  device and ignores NOWAYOUT. The old built DTB reproduces the failure. The
  board now selects userspace feeding and 60 seconds, and the driver applies
  NOWAYOUT and the DT timeout. The final kernel build checks the compiled DTB.
- Package review found missing exact local dependency pins and the Bluetooth
  control template outside the discovery contract. Both are corrected; final
  package and root artifacts are being rebuilt.
- Unrelated baseline gate failure: `pkgs/mosd/apid/ui/verify-ui-policy.sh:82`
  pipes into `grep -qx` with pipefail. The full shell gate reports
  `RESULT: FAIL (143/144 files clean, 144 scanned)`. This task does not change
  that UI script. The changed scripts pass the same scan.
- Physical BM201 installation, boot, watchdog handoff, peripherals and update
  qualification remain not tested; no device was flashed or reconfigured.

The unrelated shell failure, verbatim:

```text
FAIL: pkgs/mosd/apid/ui/verify-ui-policy.sh:82: an early-exiting grep on the right of a pipe, in a file that sets pipefail: the pipeline reports failure when the pattern IS found. Use 'grep -c ... >/dev/null'
```

## Delivery and acceptance

- Delivered `_out/s905x5m-current/image/mos-s905x5m-20260910-100627.img` with its paired
  `firmware.bin` and signed `firmware.json`, independent `kernel/`, `root/` and
  `firmware/` components, `recovery/update.img`, and generation-three
  `s905x5m-dev-3.mosupd`. The SD image contains factory generations two and one.
- Image SHA256: `f7f3c7076b3ef345dc07ba5203029bca9695fccdaa3cbb9a0df9b685c745b30a`.
- Kernel ID: `e9ac454eef15f239c2097666d716e99ed199cbd6a491317808d03c907f165674`.
- Root ID: `caf154842c88e0c3458747f335c1e292b1eb73229ecc099e68d31161ae324234`.
- Firmware ID: `d78f266629840854e685e3db3fa6343febde7487bf9ed6e300c3697d9b272d43`.
- The corrected watchdog kernel rebuilt successfully. Its exported DTB verifies
  userspace ownership and 60 seconds; its Image contains the content certificate.
  Both root/support detached signatures verify against that certificate.
- The actual final FIP key accepts the final signed FIT and rejects all five
  modified/missing/unknown-key cases. Explicit FIT key overlap/removal passes.
- The final SD image passed **104 checks, zero skipped**. An independent reader
  checked GPT CRCs, exact ranges, both native counters, zero protected slack,
  paired firmware bytes/receipt, and every signed update-archive object.
- Actual loop-disk growth to 8 GiB passed. Wrong SYSTEM/disk identities were
  refused before GPT mutation. Only DATA grew; all FIRMWARE/counter/SYSTEM bytes
  and partition identities were unchanged. Evidence: `_out/data-growth.nesaAO`.
- Actual image negatives reject an unknown metadata key, corrupted counter and
  modified external firmware payload. Final logs are in
  `_out/s905x5m-current/evidence/`; delivery hashes and source identity are in
  `SHA256SUMS` and `source-record.json` at that artifact root.
- Package acceptance checked **18 selected/optional packages and 624 owned paths**,
  exact local dependency pins, declared enablement, panel executable permissions,
  and shared Bluetooth state-mount ownership. The repository's full two-architecture
  pool gate was not substituted for this board-scoped inspection.
- Final root composition is **260 MiB installed**, below its 400 MiB budget.
  The root remained byte-identical after rebuilding the corrected package metadata.
  ARM64 smoke reports **11 pass, one executor-limited, zero failures**: crun cannot
  execute its memory-file re-exec under the BuildKit/QEMU executor. This is not
  a native crun pass or physical S7D qualification.
- Local pma-cr review covered firmware/storage trust boundaries, native runtime,
  packaging and peripheral lifecycle. The watchdog and package-contract findings
  were fixed and verified; no remaining high-confidence introduced findings.
  Verdict: **PASS**. The unrelated existing UI shell-lint failure remains recorded
  above. No compatibility reader, device write, commit or push was performed.

- complete: Signed SD development artifacts and offline acceptance completed; physical qualification remains unclaimed.
