# 20260910-1014-a3-cx3576-late-hdmi-logo CX3576 late HDMI and return-to-logo repair

- **status**: completed
- **priority**: P1
- **owner**: bkd/9qso5s2t
- **createdAt**: 2026-09-10 10:14

## Description

Repair the pinned CX3576 kernel artwork lifetime and real hotplug/VT redraw path. Preserve the existing centered YBO - Hub OS bitmap, supported display modes, and authenticated tty2. Deliver bounded software verification separately from unconfirmed board acceptance.

## ActiveForm

Delivering the verified software repair and handing coherent build and physical acceptance to A4/L2.

## Dependencies

- **blocked by**: No confirmed bench for physical acceptance; A4 owns final coherent kernel compilation.
- **blocks**: A4 coherent board artifact gate.

## Notes

- Campaign: mos-open-plans-20260910-100408; issue: 9qso5s2t; coordinator: 6064wf7l.
- Full-tier implementation and scoped local commits were explicitly approved in the user dispatch, including the dependency-satisfied message. No repeated approval is required.
- Required local L2 sync completed as 2ae10c270e61b0fbc748bbc44eae639a91eda852; 4c00a322a7fd7f65ddb11ec59ef24275c868e128 is an ancestor. No conflict or unknown change.
- Only assigned CX kernel patches/scripts/config/display tests and this tracking pair/index additions may change. Lifecycle/packaging remains with B through L2/L1.
- No push, main merge, issue creation, subagents, expensive build, or BKD done transition. Existing records stay untouched. D owns global reconciliation and change-history integration.

## Software delivery

Patch 0006 follows and preserves A1's encoder patch. It retains the generated
logo/CLUT/descriptor, restores branding on empty tty1 through real VT/HPD events,
keeps tty2 takeover and diagnostics intact, and synchronizes the visible viewport
across mapped text VTs when a framebuffer is reused. The source-extracted C
fixture includes the actual palette/centering renderer and complete affected
functions. `build.sh` now checks the three artwork ELF sections. No kernel config,
artwork, rootfs, lifecycle, authentication or packaging file was changed.

## Verification

Evidence root: `/srv/station/work/tmp/mos/9qso5s2t/`. Final command, UTC timestamp,
source identity and exit-code metadata are retained with each final gate log.
The related plan specifies the exact commands, source call chain and A4 object
handoff. No expensive kernel/root/QEMU build was started.

| Check | Result and evidence |
|---|---|
| Pinned-source behavioral/ELF RED | `red-final.log`, exit 1; real fixture failures, not compiler failure |
| GNU-patched behavioral/ELF GREEN | `green-final.log`, exit 0; sanitizer-backed display fixture |
| GNU patch application | `gnu-patch.log`, exit 0; no offsets/fuzz/rejects |
| Actual build section assertion | `build-section-red.log` rejects init object, `build-section-green.log` accepts retained object |
| Shell syntax | `syntax-final.log`, exit 0 |
| A1 regression preserved | `encoder-final.log`, exit 0 |
| Documentation suite | `docs-final.log`, exit 0 |
| Local pma-cr review | PASS, zero findings; shared policy and Python pack |

## Outstanding acceptance

| Row | Status | Required evidence/owner |
|---|---|---|
| Full ARM64 kernel/object/modpost integration | Deferred to A4 | Coherent source, actual objects, resolved config and newly built signed artifacts |
| First boot without HDMI, followed by late attach | Blocked | Confirmed bench and exact newly flashed image; monitor identity/EDID, fbdev registration and visible branding |
| Detach and reattach | Blocked | Repeated physical HPD cycles and observed display recovery |
| Authenticated tty2 and Alt+F1 return | Blocked | USB keyboard, ordinary login and visual logo restoration; no autologin |
| Single centered logo/gradient and no cursor | Blocked | Actual pixels at supported negotiated sizes |
| Startup | Blocked | Current-image serial/journal and successful health handoff |
| Reboot | Blocked | Current board reset and next-boot trace |
| Watchdog handoff | Blocked | U-Boot/Linux/systemd current-image evidence |
| Watchdog expiry | Blocked | Physical timeout reset and cause evidence |
| Real recovery | Blocked | Identified bench device and observed recovery path |
| Physical power cuts | Blocked | Identified power-cut setup and exact-image fault results |

## Delivery and cleanup

The dispatched approval covers this software deliverable and scoped local
commits. Task completion never implies hardware completion or BKD done. L2 owns
branch integration; D owns old-record/global-doc reconciliation. No main/origin
sync, push, shared lifecycle edit, issue creation, subagent or cron was used.
Task containers are exact-named, labeled `ai-agent=true` and `--rm`; their scratch
source and logs are retained outside the repository for L2/A4. No server, tmux
service, volume or network was created, and no shared cleanup sweep was run.

- Final software review: PASS with zero findings. The executable fixture checks
  30 behavior assertions plus three ELF sections; RED against the original pin
  reproduces lookup/retention, initial tty2 handling and redraw/viewport defects.
  GREEN against the GNU-applied patch passes with ASan/UBSan. The existing A1
  encoder lifecycle, changed-shell syntax and all five docs checks also pass.
- D handoff: retain this completed software record with its blocked rows;
  reconcile the old late-HDMI record, display design and outdated display bench
  wording only after L2 integration. The proposed change-history text lives in
  this node's plan. No old owned record was changed.

- complete: Bounded software repair, sanitizer regression and ELF section gates passed. A4 full integration is deferred; all physical acceptance rows remain blocked.
