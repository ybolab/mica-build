# 20260912-1329-arm64-board-builds ARM64, CX3576 and S905X5M builds

- **status**: completed
- **priority**: P1
- **owner**: worker/arm64-20260912-1329
- **createdAt**: 2026-09-12 13:29

## Description

Build virt-arm64 first, then CX3576 and S905X5M, using current source and the
development identities in `meta/`. Report progress in the active conversation
every 30 minutes, with immediate material failure updates.

## ActiveForm

Completed signed virt-arm64, CX3576 and S905X5M builds and documented acceptance boundaries.

## Dependencies

- **blocked by**: none for compilation; physical acceptance requires actual boards
- **blocks**: complete ARM board qualification

## Notes

Full-tier execution explicitly authorized by the user. No compatibility work,
parallel agents, campaign, old cron or push. Local commits remain authorized.
Use pinned Docker tools, narrow project mounts and no fixed resource caps.
Keep package compilation separate from rootfs composition and reuse successful
components with identical inputs. Retain SYSTEM 1 GiB and signing, storage,
authentication and watchdog contracts. Build success is not hardware acceptance.
S905 means the repository's `s905x5m` target.

The daemon exposes 32 CPUs and 134,314,127,360 bytes RAM; the project filesystem
has about 1.6 TiB available. Its default builder supports amd64 only. Use a
labelled pinned BuildKit daemon for ARM emulation without changing host binfmt.
Existing amd64 builders and x64 artifacts are kept. No callable conversation
scheduling interface is available; periodic reports occur during active work.

## ARM preparation fix

The component packager selected the amd64 boot-tools image for every UEFI
target, including its recorded packager identity. A regression test reproduced
the incorrect virt-arm64 firmware invocation. Select the image by EFI target
for both execution and identity; keep FIT tooling separate. Remove the two-job
Ninja override in boot tools, following the existing unrestricted-resource rule.
Docker tests passed: 10 Bun tests / 49 assertions, 15 launcher/recipe cases and
static startup archive reproducibility. The original failing route is retained
under `_out/arm64-build/`. Local review found no high-confidence defect.
ARM toolchains and virt-arm64 kernel builds are running; their source inputs
are unaffected by this packaging fix. Freeze the new clean commit before
compiling shared ARM packages and signing components.

The real ARM64 stub extraction then failed because native x64 binutils cannot
read AArch64 PE files. Install the target binutils in the aa64 tools branch
and select `aarch64-linux-gnu-objcopy` for signed initrd extraction. The recipe
regression and actual rebuilt-container PE extraction now pass. Original
format-refusal and stale test-output failures remain in the evidence directory.
All seven ARM64 builder images and virt-arm64 kernel production passed.
Main has independent document-audit/pruning edits, so production uses the clean
isolated worktree `/srv/ybolab/mos-arm64-production` at
`7f5c3d832bda6bc278ddb8fc75c0c67ee29bdbbc`; those unrelated edits are preserved.
An independent cleanup removed the initial `.tmp/arm64-production` checkout.
Recovery reused the retained kernel, native executables and Docker images after
digest checks; it did not repeat successful compilation or rotate identities.
Signed virt-arm64 kernel/support and UEFI firmware packaging passed. Shared
packages through mosd/apid passed; Podman is compiling. Full image and guest
acceptance remain pending. Evidence is in `_out/arm64-build/` in the main
checkout; new package and image artifacts are in the production worktree.

## Full ARM64 image findings

The initial full virt-arm64 image and update archive were generated. QEMU API
acceptance passed 149/149. Factory-root smoke passed 11 artifacts with crun
executor-limited under qemu-user; full-system QEMU then executed crun 1.29.1
successfully on both boots. Same-VM reboot persistence and native poweroff
passed, with guest RESET/SHUTDOWN QMP events and no watchdog reset.

Static full-image verification found two real omissions: the disabled nftables
unit and the empty MQTT enrollment directory were absent from the runtime
selection. Keep both package-owned paths in the consumer policy. Two regression
cases fail against the old policy; all 89 selection tests pass after the fix.
Main commit `3c0f4899` carries the fix. The isolated composition commit
`f6733607bb67ba547905bbc9425980cc7181a9a4` has only those two changed files
relative to package source `7f5c3d832bda`; this preserves the existing checked
composition-only reuse boundary. Reassembly and static revalidation are pending.
CX3576 kernel and U-Boot production passed; its root composition is running.

CX3576 root selection also exposed an outdated BlueZ executable path. The
pinned BlueZ 5.82-1.1 archive installs `/usr/libexec/bluetooth/bluetoothd`;
selection still required `/usr/lib/bluetooth/bluetoothd`. The real failure and
its regression are preserved; 90 selector tests now pass. Main commit
`c1fb70f4` carries the path fix. Composition source is now
`2ed144cfcf4af5482393263e4992a778c08d7a66`, still differing from package source
only in `consumers.json` and its selector test.

All 21 ARM64 package archives, including S905X5M userspace/board packages, are
frozen with 953 regular producer inputs and actual Docker tool identities.
The existing source-lineage verifier accepted the frozen receipt and the two
composition-only paths. Native startup inputs were also compared against the
preceding packaging-only commits before recording their reuse. Original
virt-arm64 static failures and runtime successes are preserved under
`_out/arm64-build/pre-closure-fix/`; fixed image composition is running.

## Corrected virt-arm64 acceptance

Image `mos-virt-arm64-20260912-142941.img` passed static verification (104
checks; the undeclared-radio Bluetooth policy check skipped), QEMU API 149/149,
and the full-system crun/reboot/persistence/poweroff checks. The smoke executor
still reports crun as limited; the full-system run closes that specific gap.
The package-source checkout initially lacked the original `mosd-build-arm64.txt`
sidecar; copying that exact producer record restored provenance checks without
recompilation. This refusal and its output remain in `missing-build-record/`.
CX3576 root composition has resumed with the corrected policy.

## CX3576 build acceptance

Corrected CX3576 root composition, signed FIT/kernel/support, firmware record,
complete image and update archive passed. Static full-image verification passed
126/126 with no skipped checks. Factory-root smoke passed 11 with crun limited
by qemu-user; this board has not been booted on physical hardware. Exact artifact
hashes are recorded in `cx3576-factory.sha256`. S905X5M kernel production started
at 14:35 UTC after the successful CX3576 sequence.

## S905X5M composition findings

Kernel `6.12.38-m100-arm64`, U-Boot and the recovery package passed production.
The recovery `update.img` SHA256 is
`8fbbcd4dca70d1950dfcb157acf21104f9f11503ef193399564891889d697949`.
A targeted selector regression found that the board's VHCI bridge, dlopen
library and calibration files were omitted; 91 tests pass after retaining the
six explicit package-owned paths (`a2b2c451` on main). The first full composition
failed earlier on missing dmsetup: its fixed Debian record selected the other
boards but not S905X5M. Add S905X5M to the existing dmsetup/libdevmapper consumers;
actual Docker package selection now returns both unchanged pinned archives.
One retry was deliberately stopped for this dependency correction; it is not
acceptance evidence. Both that interruption and the original missing-dependency
failure are retained. Composition source `0c8ffd5f6fddd905476dc47a42273235a4af886a`
continues to reuse the frozen 21-package pool; hardware/package outputs are kept.

- complete: All three signed full images and update archives built; static checks passed. virt-arm64 QEMU API and reboot lifecycle passed. Physical board acceptance remains unexecuted.

## Final results

All requested build targets completed on 2026-09-12. S905X5M static verification
passed 105/105; its factory-root smoke passed 11 with crun executor-limited.
The same shared crun package passed in the complete virt-arm64 QEMU guest;
that is not physical CX3576 or S905X5M acceptance. No physical board boot,
radio or storage stress run is claimed by this build task.

| Board | Complete image | Static verification | QEMU API |
| --- | --- | --- | --- |
| virt-arm64 | `mos-virt-arm64-20260912-142941.img` | 104 pass, 1 undeclared-radio skip | 149/149 |
| cx3576 | `mos-cx3576-20260912-143452.img` | 126/126 | Not applicable |
| s905x5m | `mos-s905x5m-20260912-145236.img` | 105/105 | Not applicable |

SYSTEM remains 1,073,741,824 bytes on all three images. Complete images and
`.mosupd` archives reside in `/srv/ybolab/mos-arm64-production/_out/arm64-build/`.
The main checkout's `_out/arm64-build/artifacts.json` records paths, byte sizes,
SHA256 digests, source identities and explicit acceptance limits. The later
S905X5M-only policy edits leave every selected virt-arm64/CX3576 consumer rule
unchanged; their already accepted roots are retained. No successful package,
kernel or U-Boot input was recompiled for the composition fixes.
