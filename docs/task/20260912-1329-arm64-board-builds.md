# 20260912-1329-arm64-board-builds ARM64, CX3576 and S905X5M builds

- **status**: in_progress
- **priority**: P1
- **owner**: worker/arm64-20260912-1329
- **createdAt**: 2026-09-12 13:29

## Description

Build virt-arm64 first, then CX3576 and S905X5M, using current source and the
development identities in `meta/`. Report progress in the active conversation
every 30 minutes, with immediate material failure updates.

## ActiveForm

Compiling the shared ARM64 Podman package before full virt-arm64 image assembly and QEMU acceptance.

## Dependencies

- **blocked by**: none for compilation; physical acceptance requires actual boards
- **blocks**: complete ARM board qualification

## Notes

Full-tier execution explicitly authorized by the user. No compatibility work,
parallel agents, campaign, old cron or push. Local commits remain authorized.
Use pinned Docker tools, narrow project mounts and no fixed resource caps.
Keep package compilation separate from rootfs composition and reuse successful
components with identical inputs. Retain SYSTEM 1 GiB and signing, storage,
authentication and watchdog contracts. Build success is not hardware acceptance.
S905 means the repository's `s905x5m` target.

The daemon exposes 32 CPUs and 134,314,127,360 bytes RAM; the project filesystem
has about 1.6 TiB available. Its default builder supports amd64 only. Use a
labelled pinned BuildKit daemon for ARM emulation without changing host binfmt.
Existing amd64 builders and x64 artifacts are kept. No callable conversation
scheduling interface is available; periodic reports occur during active work.

## ARM preparation fix

The component packager selected the amd64 boot-tools image for every UEFI
target, including its recorded packager identity. A regression test reproduced
the incorrect virt-arm64 firmware invocation. Select the image by EFI target
for both execution and identity; keep FIT tooling separate. Remove the two-job
Ninja override in boot tools, following the existing unrestricted-resource rule.
Docker tests passed: 10 Bun tests / 49 assertions, 15 launcher/recipe cases and
static startup archive reproducibility. The original failing route is retained
under `_out/arm64-build/`. Local review found no high-confidence defect.
ARM toolchains and virt-arm64 kernel builds are running; their source inputs
are unaffected by this packaging fix. Freeze the new clean commit before
compiling shared ARM packages and signing components.

The real ARM64 stub extraction then failed because native x64 binutils cannot
read AArch64 PE files. Install the target binutils in the aa64 tools branch
and select `aarch64-linux-gnu-objcopy` for signed initrd extraction. The recipe
regression and actual rebuilt-container PE extraction now pass. Original
format-refusal and stale test-output failures remain in the evidence directory.
All seven ARM64 builder images and virt-arm64 kernel production passed.
Main has independent document-audit/pruning edits, so production uses the clean
isolated worktree `/srv/ybolab/mos-arm64-production` at
`7f5c3d832bda6bc278ddb8fc75c0c67ee29bdbbc`; those unrelated edits are preserved.
An independent cleanup removed the initial `.tmp/arm64-production` checkout.
Recovery reused the retained kernel, native executables and Docker images after
digest checks; it did not repeat successful compilation or rotate identities.
Signed virt-arm64 kernel/support and UEFI firmware packaging passed. Shared
packages through mosd/apid passed; Podman is compiling. Full image and guest
acceptance remain pending. Evidence is in `_out/arm64-build/` in the main
checkout; new package and image artifacts are in the production worktree.
