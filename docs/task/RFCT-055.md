# RFCT-055 rename webd to apid: crate, binary, unit, StateDirectory, image, verifiers, health gate

- **status**: implementation complete — all mandatory project checks green
  except `make docs-verify`, which fails only on this record's deliberately
  absent index entry (see "Checks" below)
- **priority**: P1
- **owner**: ai-agent
- **createdAt**: 2026-08-19 15:20
- **claimedAt**: 2026-08-19 15:20
- **completedAt**: 2026-08-19 15:45

Campaign `l1-o7ee8v0o-20260819152009-apid`. Branch `bkd/y1yvh9ds`, base
`86cd669`.

## Description

The daemon called `webd` is renamed to `apid`. It is the API daemon; the
dashboard is what it serves. This is a pure rename: no route, settings key,
TLS behaviour, session logic, SSH logic, D-Bus interface member or HTTP
endpoint changes. The two-process architecture (`mosd` + `apid` as separate
processes) is kept unchanged; nothing is merged.

## What was renamed, by surface

### Rust workspace

- `mosd/webd/` -> `mosd/apid/` (`git mv`, history preserved). Contains
  `Cargo.toml`, `src/{auth,bus_client,config,main,routes,session,settings_api,tests,tls}.rs`,
  `tests/e2e.rs`.
- `mosd/apid/Cargo.toml`: `name = "webd"` -> `name = "apid"`.
- `mosd/Cargo.toml`: workspace `members` entry `"webd"` -> `"apid"`.
- `mosd/Cargo.lock`: regenerated by cargo (`cargo metadata`), not hand-edited.
  The `[[package]] name = "webd"` stanza becomes `name = "apid"` and sorts to
  the top of the alphabetical package list.
- Doc comments only: `mosd/mosd-settings/src/migration.rs`,
  `mosd/mosd-settings/src/model.rs`, `mosd/mosd-settings/tests/settings.rs`,
  `mosd/mosd/src/identity.rs`, `mosd/mosd/src/reconciler/sshd.rs`. The
  cross-crate reference `webd::auth::verify_password` in `identity.rs` becomes
  `apid::auth::verify_password`, following the crate rename.

### Environment variables

Renamed as part of the daemon's name surface; defaults and semantics unchanged
except `APID_STATE_DIR` (see StateDirectory below):

| before | after |
|---|---|
| `WEBD_HTTPS_ADDR` | `APID_HTTPS_ADDR` |
| `WEBD_HTTP_ADDR` | `APID_HTTP_ADDR` |
| `WEBD_STATE_DIR` | `APID_STATE_DIR` |
| `WEBD_BUS` | `APID_BUS` |

The `anyhow::bail!` message for an invalid bus value, every doc comment naming
them, `mosd/apid/src/main.rs`'s module header list and `mosd/apid/tests/e2e.rs`'s
`.env()` calls all follow.

### Two name-surface strings beyond the literal spec

Both are named here because they are the only places the rename touches a
string that crosses a process or wire boundary. Neither changes any logic.

- **Startup marker**: `WEBD_LISTENING https=<addr> http=<addr>` ->
  `APID_LISTENING ...` (`main.rs`). Its only consumer in the tree is
  `tests/e2e.rs`, which is renamed in the same commit. Renaming it is required
  by the zero-`webd` acceptance criterion.
- **Session cookie name**: `COOKIE_NAME: &str = "webd_session"` ->
  `"apid_session"` (`src/session.rs`). This is a wire-visible string, so it is
  called out explicitly rather than buried in a sed. It costs nothing
  additional: the StateDirectory decision below already regenerates the session
  signing key, so every pre-existing cookie is rejected on its MAC regardless of
  what the cookie is called. The session *logic* (HMAC-SHA256 over a 128-bit id,
  24-hour TTL, in-memory table) is byte-for-byte unchanged.

### Unit

`mosd/dist/webd.service` -> `mosd/dist/apid.service` (`git mv`). Exactly three
lines changed; everything else is byte-for-byte as it was, with no hardening
directives, no `User=`, and no reordering:

```
-Description=mos web UI daemon
+Description=mos API daemon (serves the web dashboard)
-ExecStart=/usr/bin/webd
+ExecStart=/usr/bin/apid
-StateDirectory=mos/webd
+StateDirectory=mos/apid
```

`After=network.target mosd.service` and `Wants=mosd.service` are unchanged.

### Image build wiring

- `os/rootfs/build.sh`, `os/rootfs/build-v2.sh`: staged binary and unit sourced
  from `target/aarch64-unknown-linux-gnu/release/apid` and
  `mosd/dist/apid.service`.
- `os/rootfs/Dockerfile`, `os/rootfs/Dockerfile.v2`: the `[ -f /tmp/mosd/apid ]`
  guard, both `install` lines, the `multi-user.target.wants/` enablement
  symlink, the `test -L` assertion, and the later ownership/chmod line naming
  `/rootfs/usr/lib/systemd/system/apid.service`. Comment prose follows:
  Dockerfile.v2's curl rationale at the top and the bind-mount comment near the
  end.
- `os/rootfs/overlay-v2/etc/systemd/system/var-lib-mos.mount`:
  `Before=mosd.service apid.service` and the comment naming `/var/lib/mos/apid`.
- `mosd/hack/build-aarch64.sh`: `-p mosd -p apid`, the `for name in mosd apid`
  loop, and the header comment.

### Health gate

- `os/health/mos-health`: probe c `unit_present apid.service`, every `log`/`fail`
  string, and the header comment listing probe order. The probed URL
  `https://127.0.0.1/healthz` is **unchanged** (4 occurrences, verified).
- `os/rootfs/overlay-v2/usr/lib/mos/mos-health`: restaged via
  `bash os/health/sync-overlay.sh`, never hand-edited; `cmp` confirms it is
  byte-identical to `os/health/mos-health`.
- `os/health/test.sh`: `FAKE_UNITS` default, the three case names
  (`apid-down`, `apid-absent`, and the `FAKE_UNITS=apid.service` case), the
  `check` descriptions, and the `grep -q 'probe apid: SKIP'` assertion string.

### Docs

- `README.md`: daemon name, plus one new line explaining it —
  "`apid` is the API daemon; the web dashboard is what it serves."
- `os/rootfs/README.md`: the curl-package rationale and the `/var` precious-path
  paragraph.

## The StateDirectory decision

`StateDirectory=mos/webd` -> `mos/apid` in the unit; the `config.rs` default
`/var/lib/mos/webd` -> `/var/lib/mos/apid`.

**Decided: accept the regeneration. No migration is written.** No
`ExecStartPre`, no oneshot, no directory-migration logic of any kind exists in
this change.

On a device updated across this change, systemd creates a fresh empty
`/var/lib/mos/apid`. What a fielded device **loses**:

- the self-signed TLS certificate and its key regenerate, so operators see the
  browser certificate warning once more and must re-accept the exception;
- the session signing key is new, so every existing browser session is
  invalidated and every operator must log in again.

What a fielded device **does not lose**:

- **the admin password hash is unaffected.** It lives in the settings tree at
  `access.webAdmin.password_hash`, persisted in `/var/lib/mos/settings.toml`,
  not in the daemon's `StateDirectory`. Operators log in again with the password
  they already have; nobody is locked out.

The old `/var/lib/mos/webd` is orphaned on already-deployed devices, and that is
the intended outcome, not an oversight. This is acceptable because every device
today is a dev unit, a self-signed certificate regenerating is not a security
event, and a migration path is code that exists only to serve a window that
closes.

## D-Bus policy verification

`mosd/dist/com.mos.mosd.conf` needed **no functional change**: apid is a bus
*client* running as root, not a name owner, so it is already covered by the
`<policy user="root">` stanza. This was proven, not assumed.

Evidence gathered on the base commit before any edit:

- The XML comment opens on line 4 (`<!-- com.mos.mosd is a ROOT-ONLY bus name`)
  and closes at the end of line 61 (`... send_destination=. -->`).
- `grep -n webd mosd/dist/com.mos.mosd.conf` returned exactly four lines: **13,
  33, 36, 58** — every one inside the 4..61 comment span.
- The two `<policy>` stanzas are lines 63–66 (`context="default"`) and 68–72
  (`user="root"`). `sed -n '62,$p' ... | grep -c webd` returned **0**.

So the only occurrences were prose, and the prose was updated in place
(`sed -i '4,61s/webd/apid/g'`, 4 substitutions, all inside the comment): the
"apid.service and the boot health gate both run as root" sentence, the EXTENSION
POINT `<policy user="apid">` example and its surrounding sentence, and the
DEFERRED paragraph's "a non-root apid needs GetSettings and GetState".

Confirmed after the edit: `sed -n '62,$p' ... | grep -c 'webd\|apid'` returns
**0** — the policy stanzas are byte-identical. `make os-dbus-policy-test` passes
10/10 against a real `dbus-daemon`, for the same reason it passed before: the
rules it exercises were never touched.

## Verifier before/after counts

The gate this task exists to satisfy: renaming an assertion is correct, deleting
one passes just as well and is silently wrong. Every count below is identical
before and after.

| metric | v1 before | v1 after | v2 before | v2 after |
|---|---|---|---|---|
| `grep -c '^\s*pass '` | 96 | **96** | 132 | **132** |
| `grep -c '^\s*fail '` | 125 | **125** | 198 | **198** |
| `grep -c '^ext_regular '` | 21 | **21** | n/a | n/a |
| `sq_regular` call sites | n/a | n/a | 41 | **41** |
| `sq_grep` call sites | n/a | n/a | 15 | **15** |
| `sq_enabled` call sites | n/a | n/a | 8 | **8** |
| `elf_is_aarch64` call sites | n/a | n/a | 2 | **2** |
| lines containing the daemon name | 16 (`webd`) | **16** (`apid`) | 13 (`webd`) | **13** (`apid`) |
| residual `webd`/`WEBD` | — | **0** | — | **0** |

**Differences accounted for.** Every count reproduces exactly. Two notes on how
the numbers were measured, neither of which is a change in the counts:

1. The task brief quoted the v2 helper baselines as 40 / 15 / 8 / 2. Measured on
   the base commit, `sq_regular` **call sites** are **41**, not 40; the other
   three match. The discrepancy is in the brief, not in the file — the base was
   never modified before measuring, and the count is unchanged at 41 after the
   rename, which is what the gate actually requires. Counting method:
   `grep -cE '^\s*sq_regular '`, which excludes the `sq_regular() {` definition
   on line 811 (no space after the name). A naive `grep -c sq_regular` returns
   42, counting the definition.
2. `os/verify-image-v2.sh` and `os/verify-image.sh` both contained the string
   `apid` **zero** times before this change, so `grep -c apid` is a clean
   measurement of the renamed surface rather than a count polluted by
   pre-existing matches.

The renamed v1 assertions (all five preserved, none dropped): `/usr/bin/apid`
regular-file, aarch64 ELF header (shell variables `WEBD_BIN` -> `APID_BIN`,
`webd_unit` -> `apid_unit`), `After=...mosd.service`, `StateDirectory=mos/apid`,
and the `multi-user.target.wants/apid.service` symlink.

The renamed v2 assertions (all six preserved): `sq_regular /usr/bin/apid`,
`elf_is_aarch64 /usr/bin/apid`, both `sq_grep` calls against
`/usr/lib/systemd/system/apid.service`, `sq_enabled apid.service`, and the M4
integration block's health-probe `pass`/`fail` strings.

All eleven fire and pass against real built images (see Checks).

### One comment rewritten rather than substituted

`os/verify-image-v2.sh` line ~1353 quoted a real commit message **verbatim**
inside quotation marks:

```
# 4c1180c, "ship curl in the v2 rootfs so the webd health probe stops
# skipping"), so its absence is now a regression, not a neutral fact.
```

Substituting inside the quotes would have falsified a historical quote — commit
`4c1180c` really does say "webd". The quotation marks were dropped and the
sentence reworded as a paraphrase that still points at the hash:

```
# 4c1180c, which shipped curl in the v2 rootfs so the apid health probe stops
# skipping), so its absence is now a regression, not a neutral fact.
```

Line count and assertion count are unaffected; this line is comment prose, not
an assertion.

## Commands run and results

| command | result |
|---|---|
| `git rev-parse HEAD` | `86cd669fa71889577f7e1ab1fab0e0e09a463dcf` — correct base |
| anchors: `mosd/webd/` dir, `mosd/dist/webd.service`, `grep -c webd os/verify-image.sh` = 16, `os/verify-image-v2.sh` = 13 | all four agreed |
| `git mv mosd/webd mosd/apid` | OK, history preserved |
| `git mv mosd/dist/webd.service mosd/dist/apid.service` | OK |
| `cargo metadata --format-version 1` | Cargo.lock regenerated; `name = "apid"` |
| `bash mosd/hack/check.sh` | **ALL CHECKS PASSED** (fmt, clippy `-D warnings`, nextest, cargo-deny: advisories/bans/licenses ok) |
| `bash mosd/hack/build-aarch64.sh` | OK — emits `.../release/mosd` and `.../release/apid` |
| `make os-image-cx3576` | OK — image assembled, installed size 214 MB (budget 400 MB) |
| `bash os/verify-image.sh` | **RESULT: PASS (136/136 checks)**; all 5 apid assertions PASS |
| `make os-image-cx3576-v2` | OK — image assembled, installed size 220 MB |
| `bash os/verify-image-v2.sh` | **RESULT: PASS (317/317 checks)**; all 6 apid assertions PASS |
| `bash os/health/sync-overlay.sh` | staged; `cmp` confirms overlay copy byte-identical |
| `make os-health-test` | **RESULT: PASS (54/54 checks)** |
| `make os-shadow-test` | **RESULT: PASS (220/220 checks)** |
| `make os-dbus-policy-test` | **RESULT: PASS (10/10 checks)** |
| `make os-repart-test` | **RESULT: PASS (18/18 checks)** |
| `make os-devkeys` | ran once: the fresh worktree had no `os/rauc/.devkeys`, which `os-bundle-cx3576` requires. Output is gitignored (`.gitignore:13`) |
| `make os-bundle-cx3576` | OK — bundle built and its inline signature verified |
| `make docs-verify` | fails **only** on this record's missing `docs/task/index.md` entry, by design |
| `grep -rn 'webd\|WEBD\|Webd' --exclude-dir=.git --exclude-dir=docs --exclude-dir=_out .` | **zero hits** |

Health-gate case counts held at the required baseline:
`grep -c '^check ' os/health/test.sh` = **49** (was 49),
`grep -c 'new_case' os/health/test.sh` = **20** (was 20).

`make docs-verify` output is reproduced verbatim in the campaign report. The
index entry for this record is deliberately not added: a later audit L3 owns
`docs/task/index.md` and adds the entries for all three task records at once.

## What could NOT be proved

- **No on-device run.** Every check here is a host-side build, a static image
  verification, or an offline test harness. Nothing was booted on real CX3576
  hardware. That `apid.service` actually starts, binds :443/:80, regenerates its
  certificate into the new `/var/lib/mos/apid`, and answers `/healthz` under
  systemd is asserted structurally (unit contents, enablement symlink, binary
  architecture) but not observed running.
- **The update-across-the-rename path was not exercised.** The StateDirectory
  consequences above — certificate regeneration, session invalidation, the
  password hash surviving in the settings tree — are reasoned from where each
  artifact is persisted, not demonstrated by installing a bundle over a device
  that had the old `webd` state directory populated.
- **`make os-dbus-policy-test` passing "for the same reason"** is established by
  the policy stanzas being byte-identical (proven by diff and by grep over lines
  62+), not by comparing the test's internal per-rule output before and after.
- **A naming collision worth flagging, not resolved here.** `README.md` still
  describes a planned `talos/` component (the directory does not exist in the
  tree). Talos Linux upstream ships its own daemon named `apid`. If that fork
  ever materialises in this repo, the name `apid` becomes ambiguous between our
  API daemon and Talos's. This is out of scope for a rename task and is recorded
  for whoever owns that integration.

## Addendum — the session cookie rename, confirmed as an accepted exception

> Added by **RFCT-058** (campaign `l1-o7ee8v0o-20260819152009-apid`) after L1
> review. **Append-only: nothing this record originally claimed is altered.**
> Everything above stands as RFCT-055 wrote it; this section finishes the record
> by attaching the confirmation that was outstanding when it was written.

**Confirmed by L1 on review: the `webd_session` -> `apid_session` cookie rename
is an accepted exception to the campaign's "no behaviour change" line.**

### Why it needed a ruling rather than a judgement call

Two campaign instructions genuinely conflicted, and neither could be satisfied
without breaking the other:

- **"No behaviour change of any kind; this task does not touch sessions."**
- **"Zero `webd` anywhere outside `docs/`."**

`COOKIE_NAME` is a `webd` string that is not in `docs/`. Honouring the second
instruction required editing a session file; honouring the first required
leaving it alone. This was a real conflict in the instructions, not an
ambiguity in how to read them.

### Why it was surfaced rather than absorbed into the sed

**A cookie name is wire-visible even when nothing about the session mechanism
moves.** It travels in `Set-Cookie` and `Cookie` headers, so it is observable
from outside the process by anything holding a session — which makes it
categorically unlike the internal identifiers renamed elsewhere in this task. A
rename that is invisible in the source tree but visible on the wire is exactly
the class of change that should be named in the record rather than buried in a
bulk substitution, whatever its practical cost turns out to be.

### The verified diff

Re-verified against the commit for this addendum
(`diff <(git show 4c3afc0^:mosd/webd/src/session.rs) <(git show 4c3afc0:mosd/apid/src/session.rs)`):
**three changed lines at two sites**, and nothing else in the file.

| Site | Change |
|---|---|
| `session.rs:18` | `pub const COOKIE_NAME: &str = "webd_session";` -> `"apid_session"` |
| `session.rs:5-6` | doc comment *"so a webd restart logs everyone out"* -> *"so an apid restart logs everyone out"* — two lines only because the article `a` -> `an` falls on the preceding wrapped line |

> **Measurement note.** The review that authorised this addendum described it as
> *two* changed lines. The precise figure is **three lines across two sites**;
> the extra line is the `a` -> `an` grammar fix forced by the wrap, and it
> carries no meaning. The substance of the review's description is exact: the
> only things that changed are **one constant and one doc comment**.

**Untouched, and confirmed untouched by reading the diff rather than by
assertion:** the HMAC-SHA256 construction, the 128-bit random session id, the
24-hour TTL, the in-memory session table, `verify`/`create`/`remove`, the
`Set-Cookie` attribute set (`Path=/; HttpOnly; Secure; SameSite=Lax`), and both
unit tests. No logic changed in this file.

### Why the cost is zero *additional*

The session-invalidation cost was **already accepted and already documented** by
this record's StateDirectory decision: `/var/lib/mos/webd` is orphaned and
`apid` generates a fresh session signing key into `/var/lib/mos/apid`. Every
pre-existing cookie therefore **fails its MAC regardless of what it is called**.
The rename adds no invalidation that was not already happening.

**The distinction that makes this an acceptable exception rather than a
precedent.** A cookie rename that invalidated live sessions *on its own* would
have been a different decision, and should have been escalated as one. This one
rides on an invalidation that was independently decided, independently
justified, and recorded above before the cookie question arose. The exception is
granted to the *ordering*, not to the principle: wire-visible renames still
require a ruling, and this one got it.
