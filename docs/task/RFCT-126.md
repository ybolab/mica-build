# RFCT-126 PLAN-015 M5: README sweep, root and extensions and the two TS suites

- **status**: pending
- **priority**: P2
- **owner**: (unclaimed)
- **createdAt**: 2026-08-26
- **plan**: PLAN-015 (M5)

Sweep `README.md` at the root, `extensions/README.md`, and the READMEs of the
`os/verify` and `os/build` TypeScript suites.

`extensions/README.md` documents an empty directory; the document states what
is there.

Each README states what the thing is and how it is run. Task IDs stay only
where they name a present-tense contract, and history goes to git.
