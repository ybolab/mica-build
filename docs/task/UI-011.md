# UI-011 Stabilize Bun V8 coverage aggregation

- **status**: pending
- **priority**: P1
- **owner**: (unassigned)
- **createdAt**: 2026-09-02 20:34

## Description

Restore a reliable coverage gate for the APID UI under the pinned Bun runtime.
All 18 Vitest files currently pass, after which `@bcoe/v8-coverage` recursively
merging Bun's V8 ranges fails with `RangeError: Maximum call stack size
exceeded`. The failure reproduces both in the prior source-mounted execution
path and in the isolated `_out` workspace, so it is not caused by UI-010.

## ActiveForm

Stabilizing Bun V8 coverage aggregation.

## Acceptance

- The pinned Bun container completes the UI test command with coverage.
- Coverage still enforces the configured statement, branch, function and line
  thresholds; the fix must not replace the gate with unit tests alone.
- The production UI build and source-isolation contract remain unchanged.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

Discovered while verifying UI-010 after three reproducible aggregation
failures. A separate no-coverage run is not an acceptable resolution.
