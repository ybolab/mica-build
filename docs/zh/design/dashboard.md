# mos 仪表盘：着陆页与信息架构

> [English](../../design/dashboard.md) | 中文
>
> PLAN-039 已交付第一版内建 SPA。英文版逐块列出的后续磁贴与数据源仍是扩展提案；本文是中文概览。

## 1. 问题与现状

`apid` 在 `/ui` 提供编译进二进制的 React/Vite SPA，所有页面只调用根相对的 `/api/...`。
`/` 优先选择有效自定义 UI，没有时进入 `/ui`。内建导航覆盖概览、网络、服务、访问和系统；
原先的 Maud 页面与表单 POST 已删除。

网络页每十秒读取一次按需快照，首先显示 networkd 当前观察到的接口数量。每行分别标出配置的
接口类型/寻址方式与实际观察到的类型、operational/carrier/address 状态和地址；只有配置却没有
实际接口、或只有实际接口却没有配置时也不会被隐藏。

## 2. 着陆页仪表盘

### 2.1 着陆页遵守的规则

1. **只读。** 没有表单、没有控件、没有破坏性动作。每个磁贴链接到做对应改动的那个页面。
2. **每个磁贴回答一个运维真的会问的问题**，并且**用那个问题的措辞**。
   一个「因为数据正好有」而存在的磁贴，不满足这条规则。
3. **每个磁贴都有定义好的红色状态，以及该状态下定义好的下一步动作。**
   一个红了却没有下一步的磁贴是告警，不是仪表盘。
4. **绝不在运维会把它读成实际情况的位置上展示「配置意图」。**
   凡是 mos 只有配置值的地方，磁贴要在自己的标签里说清楚。
5. **底层子系统在这台设备上不存在的磁贴应当缺席，而不是显示为空。**
   「不存在」必须指**硬件或设置缺席**，**绝不能**指**协调器失败**。
6. **默认与服务端渲染兼容。** 每个磁贴都是请求时读取一个值并渲染进页面。
   除非明确标注，任何磁贴都不要求客户端传输通道。

每个磁贴都标注为 **(a) 今天就能做**（数据来源是已有的总线调用）或
**(b) 需要新的 mosd 工作**。

### 2.2 身份磁贴 —— 「我在看哪台机器？」

- **展示**：配置的主机名、设备身份（`deviceId`）、配置状态（`pending` / `complete`）。
- **数据来源**：`GetSettings("hostname")`、`GetSettings("provisioning")`。
- **可用性：(a) 今天就能做。** 身份是**设置**而不是实时状态，因此可达。
- **为什么它值得占据第一屏**：一个手上有多台一体机、或者浏览器里还开着昨天标签页的运维，
  首先需要知道**这是哪台机器**。

其余磁贴（网络、存储、更新、服务健康、电源）的定义见英文版。

## 3. 其余部分的信息架构

## 4. 已提案但目前还做不到的

## 5. 技术姿态

关于是否引入客户端传输通道的决定在这一节。**纯 Rust 加密姿态是被强制的**：
`tough` 锁定在 `=0.18.0`，因为 0.19+ 强依赖 aws-lc-rs，那会引入 C 构建，
而工作区的禁用清单拒绝它。

## 8. 分期交付
