// apid-api traversal fixture asset. Served to prove asset_path::resolve's
// happy path executes over the wire -- the same function whose guards the
// traversal rows in 04-readonly probe.
console.log("fixture-asset-app-js");
