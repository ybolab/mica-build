decoy -- the bundle's own etc/passwd (apid-api traversal fixture)

A request that decodes to an absolute-looking /etc/passwd must resolve to
THIS file, inside the bundle, and never to the appliance's real one. That is
the difference between joining path components and concatenating strings
(asset_path::resolve rule 3), observed over the wire.
