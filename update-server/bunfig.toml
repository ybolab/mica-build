[test]
coverageSkipTestFiles = true
coveragePathIgnorePatterns = [
  "src/index.ts",
  "src/assets.ts",
  # Drizzle schema callbacks run during SQL generation; migrations are tested.
  "src/db/schema.ts",
  "src/component-fixture.ts"
]
coverageThreshold = { lines = 0.8, functions = 0.8 }
